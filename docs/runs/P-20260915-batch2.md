# Run record — P-20260915-batch2

Commit `4ae431c` on the fork cites `orchestration/runs/P-20260915-batch2.md`;
that directory is excluded by this clone's `.git/info/exclude` (a local rule,
not the repo's `.gitignore`), so the record lives here instead.

Plan: `docs/design/feature-gap-plan.md` §2 (D2, D3, D8) + test-suite hygiene.
Baseline: `ab5fae5` (`personal/main`, already caught up to `danieljtrujillo/main`).
Result: `4ae431c`. Built in an isolated worktree; the main checkout, which
holds other people's uncommitted work, was never touched.
Lead: Fable. Workers: Opus 5 (`opus-builder` ×2, `opus-deep-builder` ×1,
`opus-reviewer` ×1).

## Tickets

| id | scope | worker | status | write set |
|---|---|---|---|---|
| T01 | piano-roll undo/redo (D8) | opus-builder | DONE | `state/pianoRollStore.ts`, `components/audio/PianoRoll.tsx`, new `state/pianoRollHistory.test.ts` |
| T02 | effect chain: bypass keeps instance (D3); unknown ids inert-but-visible (D2) | opus-deep-builder | DONE_WITH_CONCERNS → reviewed PASS_WITH_NOTES → rework DONE | `lib/rackEffects.ts`, new `lib/rackEffects.chain.test.ts` |
| T03 | `virtuosoTransform.test.ts` fails under Node (Vite `?url` import) | opus-builder | DONE_WITH_CONCERNS | `lib/soundfontEngine.ts` |

All three ran concurrently in one worktree with disjoint write sets. Every
"concern" reported was the shared-worktree `git diff --stat` showing sibling
tickets' files — expected; each ticket proved its own scope with a
path-scoped diff.

## What changed

- **T01** — `RollHistorySnapshot` of nine document slices (`notes, bpm,
  totalSteps, lowestNote, highestNote, meterMap, pickupSteps, lanes, bends`);
  view/transport slices excluded. Reference-equality subscription recorder,
  300 ms coalescing, `historyApplying` guard, coalesce-clock reset after
  undo/redo — the `editorStore` design reproduced, not a command stack.
  `activeLane` re-clamped to 0 on undo/redo when the restored step lacks it.
  Capture-phase Ctrl/Cmd+Z / Shift+Z / Ctrl+Y in the roll, gated by
  `ownsKey('piano-roll')` and a hidden-face check, with
  `stopImmediatePropagation()` so the EDIT timeline's bubble-phase binding does
  not also fire.
- **T02** — `rebuild` splits entries into renderable (known id) and inert
  (enabled + unknown id). Bypass = instance kept, params kept, skipped when
  threading (prev→next). Dispose only when an entry leaves the chain, its
  effect id changes at the same entry id, or on `handle.dispose()`.
  Never-enabled entries stay uninstantiated. `ChainHandle.inertIds?()`
  (optional, always provided) + one `console.warn` per `entryId:effectId`.
  `buildEffectChain` gained an optional 5th `opts.resolve` — the only
  production change made for testability; all 8 call sites pass 4 args.
  `liveMixer.ts` byte-identical. Modelled on ACE-Step-DAW
  `PluginEngine.setPluginBypassed` (reference only, named in a comment).
- **T03** — the static `?url` import became a memoized lazy
  `getProcessorUrl()` awaited at both `addModule` sites. Two `vite build`s
  prove the emitted worklet asset is byte-identical
  (`spessasynth_processor.min-Do8nRWPC.js`, 402,596 B); the only delta is a
  78-byte URL chunk on an already-async path.
- **Lead integration edits** — `pianoRollBend.test.ts:4` stale "no undo
  history" comment corrected; in `rackEffects.ts`, warn-once keyed on
  `entryId:effectId` and `dispose()` resets `inert` (both from the review).

## Review (T02, independent, fresh context — ticket + diff only)

`PASS_WITH_NOTES`. Graph correctness clean (rebuild-from-empty means no
dangling/double edges; same instance re-threaded on re-enable). Disposal
sites disjoint, no use-after-dispose. Contract compatible (8 call sites, 2
`instances()` consumers pre-filter on `enabled`).

| finding | severity | disposition |
|---|---|---|
| `dispose()` did not reset `inert` | minor | fixed by lead |
| warn-once keyed on `e.id` only | minor | fixed by lead (`entryId:effectId`) |
| test gaps: effect-id-change dispose, never-instantiate, second unknown id, gamma disposal, `dispose()`→`inertIds()==[]` | minor | rework to T02 builder, test file only — DONE, 219→298 lines, each new assertion mutation-checked, `rackEffects.ts` restored byte-exact (sha256 `74779ee4…`) |
| one warn per handle → one line per hosted VST per offline export | nit | accepted as intended |
| fake edge `Set` cannot see a duplicate identical `connect` (matches Web Audio) | nit | comment added in the rework |

## Verification (lead-run, converged tree, the commit gate)

- `git diff --exit-code -- frontend/src/state/liveMixer.ts` → unchanged.
- `git diff -- '*.tsx' | grep className` → 0 lines (Tailwind sweep not needed).
- `npx tsc --noEmit -p tsconfig.json` → clean.
- `npx tsx src/lib/rackEffects.chain.test.ts` → passed.
- `npx tsx src/state/pianoRollHistory.test.ts` → ok.
- `npm test` → **103 passed, 0 failed** (exit 0). Was 100 of 101 at baseline.
- T02 builder's mutation checks: five one-line reversions of the fix and
  four of the rework assertions, each caught by its intended assertion.

## Unresolved risk

- A bypass toggle still routes through `rebuild` (full re-thread), because
  `liveMixer.chainTopoSig` includes `enabled`; a surgical reconnect needs a
  `liveMixer.ts` change — deferred.
- A bypassed instance keeps processing silence until removed (intended; same
  as the ACE reference; Tracktion's `allowBypassedProcessing` is the eventual
  refinement).
- Roll key binding has no DOM test harness; correctness rests on reading
  `lib/keyScope.ts`, `MidiPanel.tsx:619`, `WaveformEditor.tsx:1421-1435`.
- `inertIds()` has no UI consumer yet — the "renders at freeze/bounce, inert
  live" badge is §3.1 / #29 work.
