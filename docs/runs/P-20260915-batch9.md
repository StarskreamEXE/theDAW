# Run record — P-20260915-batch9

Plan: `docs/design/feature-gap-plan.md` — §3.8 step 3c (offline per-track
compensation delays in `renderCore`, T18), §3.8 step 3d (pan and in-chain
automation lead by the compensated time, T19), §3.11 (one modulation
abstraction, `lib/modulation.ts`, NodeF.I. delegating, T20), §3.12 #49
(piano-roll copy/cut/paste, T21), §3.7 #45 (MIDI capture into the armed
MIDI track, T22), §3.12 #47 (semantic undo coalescing, T23). Wave 2:
#46 takes/comping, per-channel metering, #61 follow actions, punch
reachability below `2xl` + punch overlay, `EffectKnob` gesture props,
`bleed` job kind.
Baseline: `b66886c` (`personal/main`; batch 8 landed as `741b79c`). Built
in an isolated worktree `_thedaw-batch9` on `feat/batch-9`; the main
checkout, which holds other people's uncommitted work, was never touched.
Lead: Fable. Workers: Opus 5 (`opus-deep-builder` / `opus-builder` per
ticket, `opus-reviewer` per ticket, one `opus-investigator` for the recon).

Recon (one investigator, run during batch 8; summary kept with the
tickets): see `recon-batch9.md` in the ticket backup.

## Tickets

| id | scope | worker | status | write set |
|---|---|---|---|---|
| T18 | offline per-track compensation delays in `renderCore` (the live `trackCompDelays` rule reused) | opus-deep-builder | DONE_WITH_CONCERNS; review PASS_WITH_NOTES; rework DONE | `lib/renderCore.ts` + test, `scripts/ab-bounce/` |
| T19 | pan / in-chain automation lead live: `laneEnvelopeEvents` `delaySec`, `entryPrefixLatencies` | opus-deep-builder | DONE; review PASS_WITH_NOTES; rework DONE | `state/liveMixer.ts` + `liveMixer.envelope.test.ts` |
| T19b | the offline mirror: `renderCore.scheduleParamLane` + the FX stepping loop on T19's helpers | opus-deep-builder | DONE_WITH_CONCERNS; review PASS_WITH_NOTES; test rework DONE | `lib/renderCore.ts` (two sites + header), new `renderCore.automation.test.ts` |
| T20 | `lib/modulation.ts` (sources → targets, control-rate engine + audio-rate wiring); `nodefiLive.ts` delegates | opus-deep-builder | DONE_WITH_CONCERNS; review PASS_WITH_NOTES; rework DONE | new `lib/modulation.ts` + test, `lib/nodefiLive.ts` |
| T21 | piano-roll copy / cut / paste / duplicate via `lib/noteClipboard.ts` | opus-builder | DONE_WITH_CONCERNS; review PASS_WITH_NOTES; rework DONE | `components/audio/PianoRoll.tsx`, new `lib/noteClipboard.ts` + test |
| T22 | `lib/midiCapture.ts` (parse, pairing, seconds → steps) + the `App.tsx` mount landing MIDI takes | opus-deep-builder | DONE_WITH_CONCERNS; review PASS_WITH_NOTES; rework DONE | new `lib/midiCapture.ts` + test, `App.tsx` (one mount) |
| T23 | semantic `coalesceKey` on the history recorder (D7 / D8) | opus-builder | DONE_WITH_CONCERNS; review FAIL; rework DONE | `state/editorStore.ts`, new `state/editorStore.coalesce.test.ts` |

Wave 1 runs six builders concurrently (disjoint write sets); T19b follows T18 and T19 because it shares `renderCore.ts` with T18 (the lead split it out of T19 at dispatch).

## Wave-2 recon (five investigators + one narrow follow-up, run during wave 1; notes in the ticket backup as `recon-wave2-*.md`)

Takes/comping (#46): three optional fields on `AudioClip`
(`takeGroupId`/`takeIndex`/`takeActive`), no side map — autosave carries
them by spread, undo snapshots `clips` wholesale, `.tasmo` needs three
coordinated additions; one shared `clipAudible` predicate at the eight
renderer gate sites; `splitGroupAt` is the one missing primitive;
Tracktion's section-list comp cited as the richer alternative. Per-strip
metering: tap AFTER the comp delay (the code's own rule), one mono
`AnalyserNode` per strip, module-scope samples read by the drawer's rAF,
`BarMeter` ballistics reused; the footer has no master meter (premise
corrected). Follow actions (#61): the rule belongs on `DawClip` (the grid
never sees `AudioClip`) and rides the existing `.tasmo` clip chain; the
15 ms launch pump is the tick; an additive `at?` on `LaunchSpec` gives a
sample-exact handover. Punch below `2xl`: a right-click radio menu on the
RECORD key's PLATE (the button is `disabled:pointer-events-none` when
unarmed), `menuitemradio` added to the shared `ContextMenu`, a red band on
the lane strip gated on the crop's own predicate. Gesture boundary:
`EffectControls` is the missing conduit; `EffectKnob`/`EffectXYPad` need
the tracker; the two bespoke XY pads need two calls each. `bleed` job:
`renderToBlob` has no progress and no cancel (three awaits → three
stages); `RenderJob` becomes a union on `kind` (the `job.kind ===` chain
already narrows); `BounceRequest` does not grow. One investigator
reported the `oss-refs/` clones absent — an artefact of Glob skipping the
ignored directory; they are on disk.

Tickets written: T24 metering, T25 follow actions, T26 punch, T27 gesture
boundary (wave 2a, disjoint), T46 takes/comping (wave 2b, alone), T28
bleed (wave 2c, alone in `WaveformEditor.tsx`).

## T19 — what landed (DONE)

- `liveMixer.ts` + `liveMixer.envelope.test.ts` (+360/−14).
  `laneEnvelopeEvents(…, delaySec = 0)`: the shift lives inside `toCtx`
  with `toTimeline` its inverse, so the `whenCtx <= now` collapse, the
  cursor spacing and the "curve already under way" degrade are unchanged;
  negative/non-finite → 0; the default call is today's list (pinned).
  `entryPrefixLatencies(entries, opts?: ChainLatencyOptions)` (prefix sum
  over `chainLatencyReport().perEntry`, first-occurrence-wins) and
  `fxLaneSampleTime(t, prefix, total) = clamp(t − prefix, 0, total)` are
  exported seams. A `refreshAutomationDelays()` cache (`panSec` + prefix
  per track, one slot for master) is signature-gated per chain and
  refreshed at the top of `syncTrackLatency` (ahead of both early
  returns) and in `applyMasterChainLive` after its full-signature gate;
  the map is rebuilt, never mutated. `scheduleLaneNative` passes the pan
  delay for `trackPan`, 0 for `trackVolume` — a map lookup per lane.
- **Lead decision, builder's deviation accepted:** the pan delay is the
  track's OWN insert-chain latency, not `TrackCompRow.latencySec` — the
  panner sits `[fx] → panner → comp → routed`, so the comp and every
  downstream bus rack are behind the param; the whole-path number would
  make pan LATE by the bus latency for a track routed through a bus with
  a latency-declaring effect. The ticket's pin (6 ms chain, no buses) is
  identical either way. The `outputLatencySec` header's residual is now
  sends only. T19b's ticket carries the exact offline calls, including
  that the suspend-time union must shift FORWARD by the entry prefix or
  a breakpoint is missed until the next one.
- Builder's `npm test` was 136/2 with both failures in T18's and T23's
  mid-edit files; its five `liveMixer.*` suites pass. Review in flight.

## T21 — what landed (DONE_WITH_CONCERNS)

- `PianoRoll.tsx` (+68/−9), NEW `lib/noteClipboard.ts` + test. A
  module-level `{ notes, anchorStep }` clipboard (in-app only — the roll's
  note has no agreed text form; a system-clipboard round trip is its own
  ticket); `Ctrl/Cmd+C/X/V/D` folded into the roll's EXISTING
  capture-phase window listener (the Z/Y one) with its `ownsKey`,
  hidden-roll and text-field guards. Insertion point: the roll has no
  edit cursor, so `isPlaying ? floor(currentStep) : lastClickStep`
  (`insertStepRef` set by grid clicks and note pointer-downs). History:
  the store has no transaction API; its subscriber snapshots on the first
  change of a 300 ms burst, so one `replaceAll` = one step; the following
  `setSelectedNote` touches no tracked slice (pinned: cut = 1 step, paste
  = 1 step, a three-write paste fails the pin). Helpers take an iterable
  of ids (a marquee later needs no change); the roll's model is a single
  `selectedNoteId`. Shortcut sheet = the `ROLL_HELP` title/sr-only text.
  Browser check on 9950: one note copied/pasted/duplicated/cut/undone;
  keys inert with the BPM field focused.
- Concerns: the browser check moved ONE note (single selection is the
  roll's model; the three-note case is pinned against the real store);
  two clipboard edits within 300 ms fold (pre-existing coalescer);
  repeated paste stacks at the same step (no auto-advance asked). The
  builder's second `npm test` showed 22 failures, all in other tickets'
  mid-edit files. Review in flight.

## T23 — what landed (DONE_WITH_CONCERNS)

- `editorStore.ts` (+178/−47), NEW `editorStore.coalesce.test.ts`.
  Three module vars beside `lastDocChangeAt`: `currentCoalesceKey`
  (consumed by the recorder so a no-op action leaks nothing),
  `lastCoalesceKey`, `coalesceContinues`; the recorder coalesces only if
  inside the window AND (`continues` OR `key === lastCoalesceKey`).
  `beginUndoStep(key?)`; `HISTORY_COALESCE_MS` and `docSnapshot`
  untouched (the automation key-set pin passes unmoved). Keyed:
  `updateTrack` (volume/pan-only updates → `track:<id>:volume|pan|
  pan+volume`), `updateClip`/`stretchClipToFit` → `clip:<id>`, `setBpm`,
  `updateBus`, `setSendGain` → `send:<from>|<to>`, the three
  `update*EffectParams` and two `set*VstRawState` → `…:fx:<entry>` /
  `…:vst:<entry>`, the automation touch quartet →
  `controlKeyForTarget(target)`, `advanceAutomationHolds` (joins the open
  step, else `automation:pass`), `beginAutomationPass`,
  `setAutomationPointCurve`/`updateAutomationPoint` →
  `automation:<lane>:curve|point`, `moveMarker`; `loadProject`/`undo`/
  `redo` call `beginUndoStep()`. D7 reproduced red (`1 !== 2`) then green.
- **Three stated deviations, lead's reading:** (1) automation writes are
  keyed by the CONTROL, not `automation:<targetKey>`, because an armed
  ride writes `updateTrack` AND `moveAutomationTouch` every frame —
  distinct keys would give one step per frame; (2) a write with no key is
  ANONYMOUS (null), not uniquely keyed — `editorStore.test.ts:136` pins
  two raw writes folding and `placeTakes` lands N takes after one
  `beginUndoStep()`; (3) `{ coalesce: true }` stays a join-whatever-is-
  open override — `editorStore.routing.test.ts:307-313` pins a bus fader
  + send knob in one capture as one step. All three are evidence-backed
  and handed to the reviewer to confirm. Still merges: anonymous writes
  with each other inside 300 ms (two `addClipToTrack`); still splits:
  truly frame-interleaved gestures on two faders (a linear snapshot stack
  cannot do otherwise). Full `npm test` deferred to the gate.

## T22 — what landed (DONE_WITH_CONCERNS)

- NEW `lib/midiCapture.ts` + test (type-only imports — runs under plain
  `tsx` with no DOM, no store graph, no device), `App.tsx` (+57/−1: one
  import block, one `useEffect` mount returning the disposer).
  `parseMidiMessage`, `createNoteCapture({ now })` pairing per (channel,
  note) on the injected transport clock (never `msg.t`), held notes
  closed at `close()`; `notesToRoll` at 16ths (`STEPS_PER_BEAT = 4`,
  matching `renderStepNotesToBlob`'s `60/bpm/4`), nearest-step edges,
  one-step minimum. A pass lands one `addClipToTrack` per capturing
  track (`sourceKind: 'piano-roll'`, `sourceBpm` = the document tempo,
  `sourceTotalSteps`, `instrumentProgram`, label `MIDI take N`) then
  `renderStepNotesToBlob` → `applyClipRender` with `renderedProgram`
  stamped (without it `WaveformEditor.tsx:2134` would re-render and
  overwrite the fresh blob) — all under one `beginUndoStep()`.
- **Armed-track rule (builder's, lead accepts):** capture iff
  `instrumentProgram !== undefined` OR the latest clip is a MIDI clip; an
  EMPTY armed track does NOT capture — the mic engine already lands a
  take on every armed track, so an empty track capturing both would drop
  two clips at one second from one press. `capturesMidi()` exported and
  pinned for five cases.
- **Deviation (the concern):** `punchWindow()` is module-private in
  `recordingStore.ts`; instead of NEEDS_CONTEXT the builder restated the
  five-line derivation in the `App.tsx` wiring from the exported
  `punchMode()` + the editor's loop fields (same open edges). Lead
  decision: accepted for this wave; T26 exports `punchWindowFrom` from
  the store this batch, and the `App.tsx` block collapses onto it then
  (recorded as a T26 must-do). Second notice `RECORD: no MIDI inside the
  punch window` added — "no MIDI received" would be false when notes
  were punched away.
- TDD disclosure: core written before the test; a six-mutant check
  (held-note flush, velocity-0 off, transport-vs-zero timestamp, the
  armed-audio exclusion, the punch crop, the single undo step) → 6/6
  caught, file restored byte-identical. `npm test` 138/0 exit 0 at the
  builder's final run; tsc 0; greps 0 on `App.tsx`; LF. Browser check on
  5410 (Playwright driving the app's own module instances via Vite
  `import()`): two armed tracks (one with `instrumentProgram: 0`),
  playhead at 8 s, a synthetic burst with a velocity-0 off and a note
  held through STOP → one `MIDI take 1` at 8 s, 1.5 s, 12 steps, three
  notes, 370 kB blob, 240 peak bins; the audio track received nothing;
  undo removed it in one step; an empty pass posted the notice. No
  `oss-refs/` file opened (an unearned ACE citation was removed).
- Remaining: N armed MIDI tracks all take the same stream (no per-track
  MIDI input routing exists); tempo ramps inside a pass are not followed
  (single `sourceBpm`, as the roll itself); a pass with the transport
  NOT rolling collapses to step 0 — the audio path has the
  `transportRolled` repair, the MIDI path has none (follow-up ticket).

## T21 review (fresh reviewer, ticket + diff + both new files): PASS_WITH_NOTES

Verified: keys ride the existing capture-phase effect with the text-field
guard ahead of `ownsKey`; cut and paste are one `replaceAll` each and
`setSelectedNote` adds no step; both clamp edges; duplicate after the
selection's end; ids match the store's `uid()`; `ROLL_HELP` is the roll's
only sheet; nothing outside the write set. Findings → one rework (in
flight): **major** — when the roll owns the key but has nothing to act
on it returns without `stopImmediatePropagation`, so `WaveformEditor`'s
bubble-phase window listener (no `ownsKey` gate) cuts/pastes/duplicates
CLIPS instead — stop propagation unconditionally once the roll owns the
key; **minor** — a second `Ctrl+V` stacks an identical set in place
(advance the insertion point past the block); `Ctrl+C` claims the key
over a real text selection (add the collapsed-selection escape the
timeline uses); `insertStepRef` survives a clip switch and a paste can
land off-screen (reset on `editingClipId`, scroll into view); a nit on
the help wording. Noted, not changed: two clipboard ops within 300 ms
fold into one step (the store's coalescer, pre-existing).
- **T21 rework: DONE** (`PianoRoll.tsx` +80/−9; libs unchanged).
  `preventDefault` + `stopImmediatePropagation` fire for `c/x/v/d` right
  after the `ownsKey` + visibility gate, before any store read, so every
  later bail is a silent no-op EDIT's listener never sees; the one escape
  is a live text selection on `c`/`x`; paste advances the insertion point
  past the block (`max(step + length)`), duplicate unchanged; the ref
  resets on `editingClipId`; scroll-into-view SKIPPED and stated — the
  roll has no horizontal reveal routine (only the one-shot vertical
  centering and the zoom `scrollLeft` maths); help text names the
  insertion point. tsc 0 on its files, helper test green, greps 0. Not
  re-checked in the browser after the rework (code + typecheck only).

## T19 review (fresh reviewer, ticket + diff): PASS_WITH_NOTES

Verified by hand-evaluation: `delaySec` gated finite-positive and applied
only in `toCtx`/`toTimeline`, the anchor unconditionally at `now`, the
degrade path arithmetically unchanged (the reviewer reproduced the test's
numbers), default-0 byte-identical (`a − a` is `+0`); the pan sign and
magnitude — audio entering at `n` reaches the panner at `n + L`, `L` the
own insert-chain total, so `+delaySec` is right and `TrackCompRow.
latencySec` would over-delay by the downstream bus chains (the builder's
deviation confirmed correct); prefix sums strictly before each entry,
bypassed/unresolvable entries contribute 0 while still receiving their
prefix, first-wins can only under-delay; the cache rebuilt into a fresh
Map so a removed track cannot linger, warm before the first envelope
(`start()` reaches `syncTrackLatency` before `scheduleAutomation`); the
hot path is one `Map.get` + one index; T18's imports untouched; only the
two files in the diff. Findings → one rework (in flight): **minor** —
the header's residual clause is wrong for the MASTER rack: its input is
the post-comp sum, `maxSec` behind the transport, so a `masterFx` lane is
still early by `maxSec` after this ticket (the sampling stays; the clause
must say so), and "a lane on a bus's rack" is not a thing the frame
accepts; **nits** — `delaySec` is baked in at schedule time (a chain
change mid-playback keeps the old shift until the next play/seek; the
existing re-arm policy), and `applyMasterChainLive` re-stringifies every
track chain on a master knob drag (split a master-only refresh). Test gap
noted: the module-private wiring (`panDelaySecFor`, the master-slot
refresh) is unpinned; the two pure pieces are.
- **T19 rework: DONE** (`liveMixer.ts` only, +225/−14 total; no
  behaviour change so no new pin). The header now names two residuals: a
  SEND, and a MASTER-rack lane's remaining `maxSec` (wiring verified in
  code: `masterChain = buildEffectChain(ctx, masterBus, …)` and routing
  lands on `masterBus`, post-comp), and states that adding `maxSec` to the
  master prefix is a design call deliberately not taken (the offline
  equivalent is the render trim — T19b's header should point at
  `trimLeadingSec`, not `outputLatencySec()`). The bus-rack phrase is
  gone. `scheduleLaneNative`'s docblock records that `delaySec` is baked
  in at schedule time. `refreshMasterAutomationDelays()` split out and
  called from `applyMasterChainLive`; the track pass calls it too. tsc
  clean; the three `liveMixer.*` suites green.
- **T19 addendum (from T18's finding): DONE.** `insertCompNode`'s only
  production caller is `buildTrackNodes`, which always passes the strip's
  `StereoPannerNode`, whose output is hard-coded to exactly 2 channels
  (W3C Web Audio §1.30 / §1.30.4, fetched 2026-09-15) — so the live comp's
  input count cannot drop and T18's tail-loss cannot occur live today.
  On the builder's own recommendation the lead had it pinned anyway:
  `comp.channelCount = 2; comp.channelCountMode = 'explicit'` before any
  connect (a no-op on the current graph; the offline renderer already
  has a panner-less mono-stem strip, so the day a live strip does the
  same the node is already safe). Pinned in `liveMixer.latency.test.ts`
  with the fake seeded at `0`/`'unset'` so a default cannot satisfy it;
  non-vacuity proved by commenting the assignments out (assertion
  fails) and restoring. `liveMixer.ts` now +250/−14; the three suites
  green; tsc clean.

## T20 — what landed (DONE_WITH_CONCERNS)

- NEW `lib/modulation.ts` + test; `lib/nodefiLive.ts` (+103/−34, delegate
  only). Pure core: the `ModSource`/`ModTarget`/`ModRoute` types,
  `sampleSource`, `applyRoutes`, `targetKey`, `spanOf`, `normalizedDepth`,
  `gridBeats`/`syncedRateHz` (delegating to `beatClock.gridSec/beatSec` —
  no second tempo owner; 7/4 pinned), `shRandom` (a pure hash of the step
  index, so a given time always samples the same level whatever the tick
  cadence); lane sources through the injected `getLane` +
  `automationModes.sampleCurve` (no store import); macro/lane clamp to
  `[0,1]`. Behind the seam: `createModEngine` (33 ms ticker, sources
  evaluated once per tick, routes summed per target, one `apply` per
  target per tick, no write when unchanged) and `connectAudioRate` (the
  Soundscape `osc → gain(depth) → AudioParam` idiom with a
  `ConstantSourceNode` DC leg for unipolar routes; `null` for anything it
  cannot drive). NodeF.I. parity: `amount = 1`, `depth =
  normalizedDepth(raw, target)` so the span multiplies straight back out
  (no descriptor → ±Infinity → span 1, unclamped, bit-identical; with a
  descriptor exact to ~1 ulp, pinned at 1e-9 over 61 ticks × 4 shapes);
  shapes transcribed unchanged incl. the peak-starting triangle; rate
  still from `lfoRateHz`, not `beatClock` (a non-4/4 meter would have
  changed NodeF.I.'s rates); `syncControlMods()` rebuilds routes on arming
  and on every `updateParams`. Two stated deltas: no write when the value
  did not move; phase wraps for negative `t` (only NodeF.I.'s ~120 ms of
  pre-`t0` ticks differ). MIT notice + copyright + path in the header;
  Tracktion cited as design only, not opened.
- **Deviation (the concern), lead accepts:** NodeF.I.'s AUDIO-RATE branch
  stays on its own wiring — a mod port takes an `AudioNode`, not an
  `AudioParam` (`xfade.pos` fans one signal into two gains), and the
  signal is the lfo node's own shared oscillator started on the common
  `t0` and re-tuned live; a per-route oscillator would break sharing,
  phase-lock and re-tuning. `connectAudioRate` is real, exported and
  tested for `lfo → AudioParam` routes. Unifying that branch needs a
  third `ModTarget` variant (a node-port hookup) and a shared-oscillator
  mode — a follow-up ticket, not this one.
- tsc clean, `npm test` 138/0 exit 0, LF, banned word 0. Manual check on
  2877 (Playwright, a synthetic 0.5 s sine served through a route stub —
  no library, no backend): `stem → Rack FX(gater) → Live Out` with
  `LFO(sine 2 Hz, depth 0.4) → rack.mod` → 42 writes/s, symmetric gater
  pairs riding `0.5 ± wave·0.4`; `updateParams` to square/6 Hz/0.2 mid-
  performance re-aimed the route (`0.85/0.15`); `modParam: ''` → 0 writes;
  `stop()` → 0 writes. The engine was also driven against a real
  `getRackEffect('gater')` on a running context with the real
  `setInterval`: ≈33 ms ticks, all in `[0,1]`, zero after `dispose()`.
  No UI (the mod-matrix panel is a later ticket). Review in flight.

## T23 review (fresh reviewer, ticket + diff + the new suite): FAIL

Verified: all three deviations are evidence-backed (`writeFader`/
`writeFxParams` write the track and the lane every frame;
`editorStore.test.ts:133-139` + `placeTakes` pin anonymous folding;
`editorStore.routing.test.ts:300-319` pins the bus-fader + send-knob join);
D7/D8 pins bite against the old recorder; `loadProject`/`undo`/`redo`
reset through `beginUndoStep()`; `HISTORY_COALESCE_MS` and `docSnapshot`
byte-identical; no `resizeClip*` exists (trim flows through `updateClip`);
`setLoopRegion`/`setTrackHeight` are outside `docSnapshot`; only the two
files changed. Findings → one rework (in flight): **blocker** — the
recorder consumes the key/flag BELOW the slice-equality early return, so
an action that sets them and writes nothing leaks them onto the next
unrelated write (`moveAutomationTouch` with no hold returns `{}`;
`advanceAutomationHolds` sets `coalesceContinues = true` BEFORE its idle
short-circuit that returns `s` itself, so no listener fires and the flag
is never consumed) — once `continues` leaks, the next write of any kind
merges into the open step; **major** — a hold frame after an ANONYMOUS
step (a menu action mid-pass) folds into it and every later frame keeps
folding, so one undo of the marker rolls back the rest of the pass;
**minor** — `updateBus` keys `bus:<id>` for volume and pan alike; the
deliberate anonymous-fold residual is described but unpinned; **nit** —
the D8 pin is back-to-back, not interleaved (a linear snapshot stack
cannot do better — correct). Not run by the reviewer: any suite.
- **T23 rework: DONE** (`editorStore.ts` +191/−47). The read-and-clear
  of key/flag moved to the TOP of the subscriber, above the slice-
  equality return, so every notification consumes them; `advance
  AutomationHolds` arms only when `automationHolds` is non-empty and
  joins only a NAMED open step (`lastCoalesceKey !== null ?
  coalesceWithOpenStep : coalesceAs('automation:pass')`); `updateBus`
  keys `bus:<id>:<params>`; the anonymous-fold residual is pinned. Five
  new pins: no-op `moveAutomationTouch` → ride → clip move = 2; idle
  holds → marker → ride = 2; touch → 5 frames → `addMarker` → 5 frames =
  3 and undoing the tail keeps the marker; bus ride then rename = 2;
  two anonymous actions = 1. All five `editorStore*` suites green; tsc
  clean; the lead re-ran the coalesce and automation suites (green) and
  confirmed the consume sits at :2055-2058 ahead of the slice compare.

## T22 review (fresh reviewer, ticket + diff + both new files): PASS_WITH_NOTES

Verified: parse (status/channel nibbles, velocity-0 → off, CC/other, no
running status needed for `midiBus`), pairing per (channel, note) on the
injected `now()` with `msg.t` never read, held notes closed at `close()`;
16ths match `renderStepNotesToBlob`'s grid and the `PianoNote` shape of
the real producer; `isMidiCaptureClip` character-identical to
`liveMixer.isMidiClip`; one `beginUndoStep()` per pass, `applyClipRender`
history-exempt; the mount is StrictMode-safe (the disposer unsubscribes
both) and a pass already recording at mount is not adopted; the punch
restatement is byte-equivalent to the private pair; no store in the diff.
Findings → one rework (in flight): **major** — the placeholder clip
lands a ZERO-BYTE blob with no peaks, so the timeline's decode-peaks
effect logs `Peak decode failed` on every pass until the render lands
(a silent WAV / seeded peaks); **major, design gap not fixable in the
write set** — the mic engine records EVERY armed track, so an armed
instrument track lands a mic take AND a MIDI take (the mic-side skip
for `capturesMidi()` tracks is handed to T26, `recordingStore`'s owner in
wave 2a); **minor** — a punched MIDI take is destructive (rebuilt from
the cropped notes, `offsetIntoSource` 0) unlike an audio take — stated,
not changed; a track without `instrumentProgram` gets no
`renderedProgram` so the soundfont fallback re-renders the fresh clip
(resolve the program before the add, `ensureSoundfontReady`); the
store freezes its punch window at PRESS while the capture reads at the
`recording` flip (T26 exports the frozen pass window). Nits: `bpm`
clamp asymmetry unreachable; `postStatus` de-dupes an identical line.
- **T22 rework: DONE** (`App.tsx` +69/−1 total). The placeholder is a
  real 0.1 s silent WAV (8864 bytes, `decodeAudioData` ok) plus
  `peaks: new Float32Array(240)`, so the decode-peaks effect skips it —
  zero `Peak decode failed` entries across a browser pass on 5411. The
  `capturesMidi` doc carries a `KNOWN GAP` section naming the mic-side
  double take and the T26 hand-off; the crop comment states the
  destructive divergence. Two new deps `globalProgram` /
  `ensureSoundfontReady`: the program is resolved BEFORE the add
  (`pass.program ?? globalProgram()`), `renderedProgram` stamped with
  what rendered, the clip's `instrumentProgram` stays the track's own so
  it keeps following the picker (browser: soundfont on, `activeProgram
  40`, no track program → `renderedProgram: 40`, no re-render).
  `soundfontEngine` was already in `App.tsx`'s eager graph (measured).
  The punch block notes press-vs-flip timing pending T26's export.
  12/12 mutants caught, file restored; tsc 0; greps 0; LF.

## T20 review (fresh reviewer, ticket + diff + both new files + the Soundscape source): PASS_WITH_NOTES

Verified: the four shapes transcribed verbatim incl. the peak-starting
triangle, rate from `lfoRateHz` never `beatClock`, unmapped shapes fall
to sine as before; no descriptor → span 1 and a no-op clamp; `base` and
`apply` read `latestNodes` at call time; `syncControlMods` on arming and
from `updateParams` (every param edit reaches it; an edge/node change
stops LIVE outright so route indices are stable); `stop()` →
`dispose()` clears the interval, runs every audio disposer, empties the
memo; the pure core has no store import; `connectAudioRate` matches the
Soundscape idiom with nothing textual shared; Tracktion cited design-
only. The audio-rate deviation ACCEPTED on the code: `modConnect.pos`
takes an AudioNode and fans into two gains with an inverter, the `lfo`
node's oscillator is shared, started on `t0` and re-tuned live — all
four claims in the in-code rationale hold. Findings → one rework (in
flight): **minor, a third behaviour delta** — `updateParams` pushes the
static params through `rack.setParams` (wiping the modulated key) then
`syncControlMods()` without clearing the engine's no-write memo, so a
square or a clamped sine can stall at the base for half a cycle after
any inspector edit (add `invalidate`, call it from `updateParams`);
**minor** — the MIT notice is truncated after "WITHOUT WARRANTY OF ANY
KIND." (paste the rest verbatim); the unipolar DC leg is untested (the
fake ctx has no `createConstantSource`); **nits** — the first route's
bounds win a shared target key's clamp (a mod-matrix trap; comment it);
"bit-identical" overstates a ~1e-13 round-trip. No `nodefiLive` test
exists anywhere, so `syncControlMods`/teardown rest on the arithmetic
transcription plus the builder's manual check.
- **T20 rework: DONE** (`nodefiLive.ts` +119/−34 total). `ModEngine.
  invalidate(key?)` drops one or all memo entries; `updateParams` calls
  it right after the static `apply`, and the stall was reproduced then
  cleared in the real app (0.5 Hz square, depth 0.4: an inspector edit
  reset `depth` to 0.5, the next tick restored 0.1). The delegation
  header names both deltas. MIT notice completed verbatim from
  `oss-refs/soundscape/LICENSE:1-21` with SPDX + path. The fake ctx grew
  `createConstantSource`; a `bipolar: false` case pins gain 0.15, offset
  0.15, both wires, both stops, idempotent disposer, `dispose()` teardown
  of the DC leg, and `null` without the constructor. `applyRoutes` carries
  the shared-key trap comment; the identity claim is now "exact without a
  descriptor, ~1e-13 with one". tsc clean; suite green; LF.

## T18 — what landed (DONE_WITH_CONCERNS; 138/0; ab-bounce PASS)

- `renderCore.ts` + `renderCore.routing.test.ts`, `scripts/ab-bounce/
  main.ts` + README (+612/−111). In the master scope every built strip
  gets a `DelayNode` holding `maxSec − own` between its tail and its
  registration, computed from ONE `trackCompDelays` row set that now
  feeds both the comps and the trim (`renderLatencySec` takes the rows;
  they cannot disagree); `latency + comp == maxSec` on every path, so
  `trimLeadingSec(maxSec)` lands every track. Buses, stems (single
  chain → no node) and selections untouched; the comps degrade with
  T14's `routed` gate. Header/`trimLeadingSec` docs state alignment is
  complete for the master scope; residuals = sends and the master
  rack/device latency. No `oss-refs/` file opened.
- **Three stated deviations, lead accepts:** (1) comps ROUNDED to whole
  samples — a fractional `DelayNode` interpolates (measured: 264.6
  frames split an impulse 0.398/0.602, 265 moved it exactly) and the
  trim already rounds, so raw seconds could never be bit-identical; the
  value is set, never ramped; residual inter-track skew < half a sample
  (stated in the header). (2) Comps inserted only when some strip needs
  one, then on EVERY strip incl. 0 on the slowest — keeps
  `renderCore.test.ts`'s node-for-node pins (one audible strip per
  master case) and a latency-free project byte-identical. (3) ab-bounce
  case B's reference rebuilt as `alignedLegacyMix` (one legacy render
  per latency group, shifted by its own latency, summed) because per-
  track compensation makes B a different file; new case E pins the
  per-track case bit-identical over 1 323 000 samples (dry shifted 0,
  compressed 0.006); B stays ≤1e-6 (one float32 ULP; its reference
  convolves the master reverb per part). A panner-less master strip
  gets a unity `GainNode` tail so it can be compensated (pinned).
- **Defect found and fixed on the way:** a comp `DelayNode` on the
  default `channelCountMode: 'max'` reallocates its delay lines when the
  strip's sources go inactive and LOSES the buffered tail — measured
  max |Δ| 0.327 over the last 247 samples of every clip; pinned
  `channelCount = BOUNCE_CHANNELS`, `channelCountMode = 'explicit'`
  (delta 0 across the render; case E is the pin). The live
  `insertCompNode` uses the default mode → handed to T19's builder as a
  bounded addendum (its comp follows the StereoPannerNode, whose output
  is always 2-channel, so the hazard may not exist live — verify, then
  comment or pin). The selection scope stays uncompensated (its only
  caller renders `includeFx: false`; pinned as behaviour).
- TDD: with the splice disabled the suite fails at "the panner feeds the
  comp delay"; with the value forced to 0 at "the dry track holds the
  6 ms". tsc 0, `lint:scripts` 0, `renderCore.test.ts` ok (unmodified),
  `npm test` 138/0, ab-bounce PASS. Hit no turn limit (98 tool uses,
  46 min). Review in flight; T19b dispatched on the settled file.
- **T18 rework: DONE** (ab-bounce PASS, E BIT-IDENTICAL, B ≤1e-6;
  139/0). The master-scope context is sized `outLength + ceil(padSec ·
  sr)` with `padSec` an upper bound computed before the context exists
  (every track in scope, mute/solo ignored, buses only when
  `graphOrders` succeeds — one helper now answers both `topoOrder`
  questions so padding and comp rows agree); `trimLeadingSec` drops the
  overshoot so the file's length contract is unchanged; pinned (context
  = window + 6, file = window, last sample real not zero-fill) and end-
  to-end by case E's new `S2-tail` clip ending on the render's last
  sample (with `padSec` forced 0: E 6.2e-1 OVER GATE). Stems and
  selections deliberately NOT padded — padding a stem put case C over
  the gate against the legacy body, which truncates in the same place;
  that is T14's behaviour, now pinned rather than accidental (follow-up
  call: whether C's reference should move). Header: half a sample per
  track, one sample between two. **Channel-count claim reconciled with
  a correction to the reviewer's premise:** the loss WAS measured
  through a `StereoPannerNode` (case E has always been `includeTrackMix:
  true`; before the pin E read 2.25e-1 OVER GATE; a hand-built `source →
  gain → panner → delay` reproduces 3.27e-1 over 247 samples, identical
  to the gain-only graph) — what drops is the count the delay computes
  from an input it no longer considers ACTIVE, not the panner's output
  width; comment + README name both configurations; unit pin with the
  fake seeded `0`/`'unset'`. Fractional-rate pin at 44 100 (264.6 →
  265/44100). The node-for-node comment names the `includeTrackMix:
  false` master exception. `alignedLegacyMix` throws if an unmuted
  track is in no group, and pre-shifts each group's `trackPan` points
  by `ownSec` (the T19b reference fix) — case B back to ≤1e-6. T19b's
  regions untouched. The live comp is pinned too (T19 addendum), so
  the builder's second concern is already closed; the T19 builder's
  "cannot occur live" reasoning is therefore superseded by T18's
  measurement — the pin, not the panner, is the guarantee.

## T18 review (fresh reviewer, ticket + diff): PASS_WITH_NOTES

Verified: one `trackCompDelays` call feeds both the comps and
`renderLatencySec` (reduced to a pure max over the rows); values set,
never ramped; the insertion gate can never skip a needed strip (rows
exist for every audible track whenever `includeFx`), and with all comps
0 no node is built so `renderCore.test.ts`'s node-for-node pins hold;
routed comps pass `{ graph, buses }` only when `routedPaths` and degrade
with the cyclic-graph fallback (pinned); stems/selections build no comp
(pinned); sends now tap the comp output as live does; the unity tail is
exact and the deferred connect loop keeps masterBus input order (so
float summation order); `alignedLegacyMix` is valid (equal lengths,
re-seeded IR, linear master reverb, no master compressor) so B's ≤1e-6
is FFT ordering and E is legitimately exact. For (e): live `insertComp
Node` always follows a `StereoPannerNode` → no hazard live (matches
T19's finding; now pinned anyway). Findings → one rework, QUEUED behind
T19b because both touch `renderCore.ts`: **minor** — a compensated
track's last `maxSec` falls past the fixed render length and is never
rendered (before T18 it was rendered 6 ms early); a project over the
30 s floor ends at the last clip so every non-slowest track loses its
final ~6 ms → pad `lengthSec` by the declared-latency bound, or
document; **minor** — "under half a sample" is per track; between two
tracks it is under one sample; **minor** — case E has `includeTrackMix:
true`, so every comp follows a panner (always 2-channel) and cannot be
the configuration where the tail-loss was measured — reconcile the
measurement (panner-less master strip?) or soften the two comments;
**nits** — at `SR = 1000` the rounding is never fractional and the fake
comp swallows the channel-mode assignments (add a fractional-rate
harness + the two asserts); "node graph unchanged" excludes the
`includeTrackMix:false` master path (unused in production; wording);
`alignedLegacyMix` should assert its groups cover every unmuted track.

## T19b — what landed (DONE_WITH_CONCERNS; 139/0; ab-bounce case B red pending the harness reference)

- `renderCore.ts` (+110 over T18's delta) + NEW `renderCore.automation.
  test.ts`. `scheduleParamLane(param, lane, clamp, delaySec = 0)`; the
  pan site passes `panLeadSec(trk) = chainLatencySec(chainFor(trk), {
  sampleRate })` gated on `req.includeFx` (the chain actually BUILT —
  the same rule as the comp rows and the trim), volume 0. No double
  compensation: the offline anchor stays at 0, only future events move,
  and `trimLeadingSec` takes the same lead off the whole file. The FX
  loop computes `entryPrefixLatencies` once per chain in `groupFx`
  (carried as `prefixSec` per target), samples at `fxLaneSampleTime(tq,
  prefix, lengthSec)` (a negative time clamps to the lane at 0), and
  shifts the suspend union forward by the prefix then clips to
  `(0, lengthSec)` — one stated consequence: a `p.t = 0` breakpoint on a
  prefixed entry now gets a step of its own at `0 + prefix`, writing
  the value the initial state wrote (audibly inert). Header clause
  points the master-rack residual at the trim and names sends. tsc
  clean; `renderCore.automation/routing/test` and `liveMixer.envelope`
  green; `npm test` 139/0.
- **The concern (outside its write set):** ab-bounce case B is red (all
  four sub-cases, max |Δ| 3.9e-3 … 1.0e-2; A/C/D/E unchanged, E still
  BIT-IDENTICAL). Isolated in three runs (baseline PASS; full change
  FAIL; pan lead alone disabled → PASS at baseline numbers): the pan
  lead is the sole cause and the FX half is A/B-neutral. Cause: the
  harness's `alignedLegacyMix` shifts each group's whole rendered
  buffer by `ownSec`, moving the legacy-placed pan envelope WITH the
  audio, so the reference still has `pan(y + 0.006)` on audio from `y`
  while the core now has `pan(y)` — the intended change measured
  against a reference that encodes the old placement. Fix handed to
  T18's rework (the harness is T18's file): pre-shift the group's
  `trackPan` points by `ownSec` before the legacy render (valid for B
  because every track in the compressed group has one chain latency and
  no bus). Review in flight; T18's rework now carries both.

## T19b review (fresh reviewer, ticket + the full `renderCore.ts` diff + the new suite; hit its 25-turn limit once and wrote from what it read): PASS_WITH_NOTES

Verified: `panLeadSec` is `chainLatencySec` over the chain handed to
`buildChain` (bypassed/unresolvable entries zeroed), not the comp row —
and the sign: audio from `y` reaches the panner at `y + own`, the
envelope writes lane(`y`) there, the file prints it at `maxSec + y`,
the trim removes `maxSec` → breakpoint `y` on audio `y`; the anchor is
unmoved; volume byte-identical; the FX read-back and the forward-shifted
union are right (the `p.t = 0` extra step is correct and does real
work — it samples the lane at `Q − prefix`); prefixes once per strip,
master lanes on the master chain; the offline pins call the same live
functions the live suite pins (1.006 / 1.994 / prefix 0), and the
reviewer re-derived the suspend lists `[Q, 2Q, 25.088]` by hand; the
header clause points at the trim and `outputLatencySec` no longer
appears in the file; T18's regions undisturbed; the case-B diagnosis
traced in the harness (T1 `[compressor, reverb]` + a pan lane spanning
−1.4→1.4 over 3.3 s ≈ 8.5e-3 per 6 ms, well over the gate; the `trackFx`
lane is on the first entry so the FX half contributes nothing) and the
pre-shift fix judged sound on four grounds. Findings → one test-only
rework (in flight): **major test gap** — every case is single-track
with no routing, so the suite cannot tell the own-chain lead from the
comp-row lead (add a bus-routed case asserting `1 + own`, not `1 + own
+ bus`); **minor** — no `masterFx` lane case; no two-latency case;
**nits** — two overlapping residual lists in the header (lead folds at
the gate or leaves); `groupFx` walks chains with no lanes. The
reviewer's one open risk — whether `chop` declares latency, which would
break `renderCore.test.ts`'s pan pins — closed by the lead: only the
compressor declares `latencySec` (`rackEffects.ts:2058`) and
`renderCore.test.ts` passes on the current tree.
- **T19b test rework: DONE** (`renderCore.automation.test.ts` only).
  `panLeadsByItsOwnChainNotByItsPathToTheMaster` — `t1 → b1 → master`,
  compressor on both chains, states both candidate figures (own 0.006,
  comp row 0.012) and asserts 1.006 not 1.012, with the bus rack proven
  built; mutating the expectation fails (`MUTANT: 1.006`).
  `aMasterLaneIsSampledBehindTheMasterChain` — master `[compressor,
  reverb]`, lane on the reverb, values `[0, Q−0.006, 2Q−0.006, 25]`.
  `eachTrackLeadsByItsOwnChain` — `[compressor]` vs `[]`, ramps at 1.006
  and 1.0, two comp delays created, neither envelope moved by them. The
  `p.t = 0` comment now says the step samples the lane at `Q − prefix`.
  Harness accepts routing/buses; a kind-filtered panner finder replaces
  index lookups. `npm test` 139/0 on the tree with T18's rework landing;
  ab-bounce deliberately not re-run mid-edit.

## Wave 1 landed: `28b1dae` on `personal/main` (2026-09-15)

Committed after the gate below; pushed. Wave 2a (T24 metering, T25
follow actions, T26 punch reachability + overlay + the MIDI-track skip,
T27 rack gesture boundary) dispatched concurrently on the committed
tree; T46 takes/comping follows alone (wave 2b); T28 bleed job alone in
`WaveformEditor.tsx` (wave 2c).

| id | scope | worker | status | write set |
|---|---|---|---|---|
| T24 | per-strip meters: `state/stripMeters.ts`, comp-tap analysers in `liveMixer.ts`, `StripMeter` in the drawer | opus-deep-builder | DONE; review PASS_WITH_NOTES; rework DONE | new `stripMeters.ts` + test, `liveMixer.ts` (4 sites + export), `MixerStrips.tsx` |
| T25 | follow actions (#61): `lib/followAction.ts`, `LaunchSpec.at?`, the grid's pump + `followRef`, `.tasmo` `follow_action` end to end | opus-deep-builder | DONE_WITH_CONCERNS; review FAIL; rework DONE | `launchQueue.ts` + test, new `followAction.ts` + test, `DawSessionGrid.tsx`, `dawImportClient.ts`, `projectClient.ts`, `tasmoToSession.ts`, `tasmo_project.py`, new `tests/test_tasmo_follow.py` |
| T26 | punch mode reachable at every width (RECORD-plate radio menu), punch-window overlay, `punchWindowFrom` + exports, the mic-side skip for MIDI tracks | opus-deep-builder | DONE; review PASS_WITH_NOTES; rework DONE | `ContextMenu.tsx` (additive), `PlayerFooter.tsx`, `recordingStore.ts` + test, `WaveformEditor.tsx` (one sibling + selector), `App.tsx` (the punch block) |
| T27 | rack SCHEMA controls report a real gesture boundary (`EffectControls` conduit, `EffectKnob`/`EffectXYPad` trackers, the two bespoke pads) | opus-builder | DONE_WITH_CONCERNS; review PASS_WITH_NOTES; rework DONE | `effects/EffectControls.tsx`, `EffectKnob.tsx`, `EffectXYPad.tsx`, `OwlPad.tsx`, `SpatializerPad.tsx`, `FxRack.tsx`, `EffectWindows.tsx`, new gesture test |
| T46 | takes + comping (#46) — wave 2b, alone | opus-deep-builder | queued | `editorStore.ts`, `recordingStore.ts`, `liveMixer.ts`, `renderCore.ts` (gates), `WaveformEditor.tsx`, `projectImport.ts`, `projectClient.ts`, `tasmo_project.py` + tests |
| T28 | the Metamorph bleed as a `bleed` render job — wave 2c, alone in `WaveformEditor.tsx` | opus-builder | queued | `renderJobs.ts` + test, `morphEngine.ts`, `WaveformEditor.tsx` |

## T27 — what landed (DONE_WITH_CONCERNS; 142/0 at the builder's full run; hit the 60-turn limit at the browser check and was resumed)

- Seven `.tsx` (+213/−52) + NEW `effects/EffectKnob.gesture.test.ts`.
  `EffectKnob` and `EffectXYPad` gain `onGestureStart/End` and their own
  `createGestureTracker` in `SlideTrack`'s shape (callbacks via refs,
  tracker created on demand and cleared by the unmount cleanup so
  StrictMode's mount → cleanup → mount does not leave it dead):
  `pointerDown()` before the press's first write, `pointerUp()` on
  up/cancel, `wheelTick()` per wheel step (knob), `key('down')` before
  the keyed write with `onKeyUp`/`onBlur` closing it, the double-click
  reset as its own pair, `dispose()` on unmount; the knob's `onKeyDown`
  now picks the write before dispatching it (step maths, multipliers,
  key set byte-identical). `EffectControls` threads both props to every
  control — knob, `SlideTrack` (the boundary it always supported), and
  a `start(); write(); end();` `discrete()` pair for the `SlidePad`
  toggle, the enum select, AND (builder's addition, lead accepts) the
  tools-row preset select and reset button, which also write params.
  `OwlPad`/`SpatializerPad`'s XY surfaces call the existing
  `onGestureStart` in `onDown` and `onGestureEnd` in `onUp` after the
  final write (OwlPad's `active: 0` gate-off lands inside the gesture),
  gated on a `wasDragging` snapshot so up-then-cancel cannot emit two
  ends. `FxRack` passes the pair bound to `entry.id` at both
  `EffectControls` sites; `EffectWindows` at the Ares-closed site; the
  three "reports nothing" comments corrected. `automationGesture.ts`
  untouched; the fallback's reason (MixView) documented on the props.
- Test: mounts the real components in jsdom + react-dom (the
  `EffectWindows.test.tsx` rig) and drives real events — drag → one
  pair with start before the first change; press-with-no-move → a pair;
  up+cancel → one end; a 3-tick wheel burst → one pair; a held arrow
  with Shift released mid-ride → one pair; unhandled key → nothing;
  blur mid-press → end; double-click → `['start','change','end']`;
  unmount mid-gesture → end. Red first (`[]` vs `['start']`). Greps 0 on
  all seven; rule 3 unaffected (no control added or relabelled); LF; no
  `oss-refs/` opened. The ticket's `gestureTracker.test.ts` command
  names a file that does not exist at baseline (the tracker's contract
  lives in `SlideTrack.test.ts`) — the builder did not create one to
  satisfy the command; lead's ticket error.
- **Concern:** the browser check was NOT completed — a lane point count
  needs a loaded clip, automation armed and the transport playing
  (`writeFxParams` bails on `!isPlaying()`), unreachable in the
  remaining budget; server on 51137 killed. The one-pass-per-drag claim
  rests on the headless suite against the real components. Follow-ups:
  `SpatializerPad`'s motion select + preset buttons and `OwlPad`'s
  program select + HOLD still write with no boundary (pre-existing;
  the same `discrete()` pair would do). Review in flight.
- Review (fresh reviewer, ticket + diff + the new suite): PASS_WITH_
  NOTES. Verified: tracker calls precede the first write and follow the
  last on both widgets (up AND cancel; the second is a tracker no-op),
  wheel/key/dblclick/unmount wiring matches `SlideTrack`; value maths,
  step sizes and key sets compared byte-wise against the pre-image;
  every control threaded, the discrete pairs stateless; both pads
  start before the press write and end after the final write with the
  `wasDragging` latch; both `FxRack` sites and the Ares-closed site
  bound; `automationGesture.ts`/`gestureTracker.ts` absent from the
  diff, the fallback intact; `writeFxParams` is synchronous so the
  discrete pair really contains the change; rule 3 unaffected; the
  suite drives the real components in jsdom. Findings → one rework (in
  flight): **minor** — the two bespoke pads have no unmount path (a
  panel unmounting mid-drag never ends the explicit group; add the
  cleanup effect); **minor, lead's** — two read-only files now state
  the opposite of the code (`automationGesture.ts:29-33` fixed by the
  lead as an integration edit; `WaveformEditor.tsx:1604-1610` after
  T26 lands, since that file is T26's this wave); **nits** — the
  pad-drag and wheel blocks assert through `bounds()` and would not
  catch a tracker call AFTER the write; a disabled knob still opens a
  gesture on dblclick/key; `memo` on `EffectKnob` was already defeated
  by inline `onChange` arrows (no regression).
- **T27 rework: DONE** (+231/−54 over the seven files). Both bespoke
  pads gained an `endRef` + unmount cleanup that ends the explicit
  group only if a drag was in flight; the pad-drag and wheel blocks now
  assert `['start','change']` order directly after the press / first
  tick; a disabled knob early-returns on dblclick and keys. tsc 0 (the
  two transient errors from T24's mid-edit files cleared); suite green.

## T24 — what landed (DONE; 143/0)

- NEW `state/stripMeters.ts` + test, `liveMixer.ts` (+103/−2),
  `MixerStrips.tsx` (+208). One leaf `AnalyserNode` per track (fed from
  `comp`) and per bus (from `output`) — no outgoing edge, so the routing
  graph, `topoOrder` and the send taps never see it; `outputNodeOf`
  unchanged. `fftSize 2048` (levelsStore's overlap reason), `channelCount
  1` explicit, `speakers` (a hard-panned full-scale track reads 6 dB
  down — stated). `meter?` optional behind a `typeof ctx.createAnalyser`
  guard so the routing test's fake context and node-count pins hold.
  `reconnectLeafTaps()` runs inside `wireRouting` after the wholesale
  disconnect and before the graph wiring — no doubled edge. One export
  `getStripMeterNodes()` over a reused scratch map. `stripMeters.ts`:
  module-scope registry, `ensure/dispose` refcounted, `sampleStripLevels
  ()` reusing its objects, a `configureStripMeters({ taps })` seam (no
  cycle; buffers sized from `tap.fftSize`). `MixerStrips`: one rAF, a
  `BarMeter` per strip outside React, DOM through refs, zeros-in decay,
  ARIA throttled to 10 Hz (widths per frame); master reuses
  `sampleChannelLevels()` with a paired `ensureMeter/disposeMeter`.
  `role="meter"` + `aria-label` + `aria-valuemin/max/now/text`; the
  `TrackInputMeter` class set. Ardour `route.cc:129-130, 4618-4640,
  5744-5752` / `meter.cc:1-21, 92` cited as design (GPL named), nothing
  copied. TDD real (`ERR_MODULE_NOT_FOUND` first). tsc clean; greps 0;
  `npm test` 143/0.
- Browser check on 45220 (Playwright, two synthesised tracks + a bus):
  the five meters read −∞ / −8.0 / −14.0 / −15.9 / −5.7 dBFS — exactly
  the arithmetic (0.5 × 0.8; 0.25 × 0.8; × 0.8 bus fader), proving each
  tap reads its own strip; a rewire while playing (`setTrackOutput`)
  kept every meter reading and moved the bus/master values with the
  topology (without `reconnectLeafTaps` every bar would have gone to
  −∞); after stop the RMS fill hit 0 % within 2 s and the peak tick fell
  at the PPM rate; closing the drawer with the Levels panel open left
  `sampleChannelLevels()` live at the same reading (the refcount).
  Analyser params read back live. Follow-ups: the analysers are created
  with the strips (constant cost whether or not the drawer is open —
  the ticket's shape; lazy creation is a refinement); the bar is
  `bg-red-500/60` (inherited palette; a product call); `liveMixer.ts:33`
  carries the banned word in pre-existing prose (not an added line).
- Review (fresh reviewer, ticket + diff + both new files): PASS_WITH_
  NOTES. Verified: track tap from `comp`, bus from `output`, never
  returned by `outputNodeOf`, never in `liveIds`, never given an
  outgoing edge; `reconnectLeafTaps()` after the three disconnect
  sweeps and before `wireRoutingGraph`; dispose paths disconnect; the
  guard keeps the routing test's pins; `renderCore` does not use
  `createBusNodes` so bounces gain no analyser; the registry is module-
  scope, refcounted, object-reusing, no zustand; one rAF, zeros-in,
  dt clamped, visibility pause; rule 3 and the class set byte-identical
  to `TrackInputMeter`; the Ardour citations real and design-only; only
  the four files. Findings → one rework (in flight): **minor** — the
  liveMixer half (leaf-ness, survival of a `wireRouting` sweep) is
  asserted nowhere (add pins with a fake ctx that implements
  `createAnalyser`); holding `ensureMeter()` from the drawer constructs
  the engine and starts the master LUFS integration, and closing the
  drawer as the last holder clears the holds/history — the ticket's
  reuse, refcount traced correct, but undocumented (a comment);
  **nits** — the reconnect-ordering comment claims doubling (Web Audio
  ignores duplicate connections; the order is still right), the mono
  comment says the setting selects a channel (it pins the down-mix the
  analyser does anyway); the master analyser is read twice per frame
  when both surfaces are open (negligible).
- **T24 rework: DONE** (`liveMixer.ts` +106, `MixerStrips.tsx` +219,
  test extended). Section 6 drives `createBusNodes` with a fake ctx
  that implements `createAnalyser`: `output.outputs === [meter]`,
  `analyser.outputs === []` (a leaf), the four settings, `disposeBusNodes`
  → one disconnect, and the no-`createAnalyser` guard path; mutation-
  checked (the leaf assertion flipped fails). `wireRouting`/
  `reconnectLeafTaps`/`buildTrackNodes` cannot be driven by the fakes
  (private, real-context) — stated; the re-attach stays covered by the
  live rewire in the browser check. The master-hold comment names the
  engine construction, the LUFS integration start and the `clearHolds`
  on last release. Both nits reworded. tsc 0; its suites green. The
  builder's `npm test` was 142/1: `contextMenuContrast.test.ts` fails
  under Node on T26's new `requestAnimationFrame` focus effect — handed
  to T26's builder as a one-line guard — DONE: the focus effect bails without a `document` and, without a frame clock, focuses synchronously (there is no layout clamp to wait for); browser path byte-identical; `contextMenuContrast` green; the builder's `npm test` 143/0.

## T26 — what landed (DONE; 143/0; hit the 100-turn limit at the browser check and was resumed)

- Six files (+493/−85). `ContextMenu.tsx`: additive `checked?` →
  `menuitemradio` + `aria-checked` (checked rows tinted), `menuitem`
  otherwise. `PlayerFooter.tsx`: a plate-level `onContextMenu` +
  `onKeyDown` (Shift+F10 / the Menu key) on both `RecordKey` homes with
  local menu state; every `PUNCH_TITLE` value incl. `off` now ends in
  "right-click for punch" on `aria-label` AND `title`; NO `aria-haspopup`
  on the button (verified null); the three stale comments rewritten;
  the 2xl select kept. A dead key has no keyboard route (a disabled
  button is not focusable; making the plate focusable would add a tab
  stop the ticket did not authorise) — stated, lead accepts.
  `recordingStore.ts`: `punchWindowFrom(punch, loop)` carries the WHOLE
  gate (the private `loopRegion()` folded in), `punchWindow()` delegates,
  `currentPassPunchWindow()` returns the window frozen at press;
  **the mic-side skip**: `syncArmed` arms the engine only with ids
  `capturesMidi` rejects (`micArmedIds`) while `armedTrackIds` stays the
  full list; `micPassOpen` (from `engine.armed().length`) lets `beginPass`
  skip `engine.start()` (which throws `nothing-armed` on an empty list)
  while `startInFlight`/`stopPending`/`startTransport`/`transportRolled`
  and `release()`'s `status:'recording'` run as before, and `finishPass`
  writes `stopping` → `placeTakes([])` → `idle` — the status sequence is
  byte-identical, which is what `midiCapture.onStatus` opens and closes
  on. Four new test blocks (+174; a re-flipped `micArmedIds` fails the
  hand-off pin). `WaveformEditor.tsx`: one `useRecordingPrefs` selector +
  one `aria-hidden pointer-events-none` sibling after the lane loop
  band, no `z` (under the `z-30` playhead). `App.tsx`: the restated
  derivation gone; `punchWindow: currentPassPunchWindow`.
- Browser check on 23616 (chrome-devtools, exact viewports): layout at
  960/1024/1280/1536 **byte-for-byte** batch 8's numbers (the select
  not rendered below 2xl; 1536: 985.6–1049.6); at 1536 on a dead key the
  plate menu opened with four `menuitemradio` rows, `aria-checked`
  `[true,false,false,false]`; `Punch in-out` flipped the select, the
  aria-label, and drew the red band at the loop band's own left/width
  (`bg-red-500/10 border-red-500/50 border-x`, z auto); loop disabled →
  0 red bands, the greyed loop band still 1; 1024: `in` → `left/right:0/
  border-l`, `out` → `left:0/width/border-r`; keyboard on a live key:
  Shift+F10 opened it anchored at the plate, Escape closed, the Menu
  key reopened with `in-out` checked. No `oss-refs/` opened.
- Lead integration edits after T26: the `KNOWN GAP` paragraph in
  `midiCapture.ts` (T22's hand-off note, now closed) rewritten as "ONE
  DEFINITION, TWO RECORDERS"; the T27-stale gesture comment in
  `WaveformEditor.tsx:1604-1610` rewritten (T27's reviewer finding 2).
  `midiCapture.test.ts` green after. Follow-ups: the LOOP button's title
  does not say the region is also the punch window; `ContextMenu` has
  no arrow-key roving and no accessible name on the menu (pre-existing,
  every caller); the RECORD key homes can be absent until a re-render
  after the EDIT tab mounts (`isEditorPlaybackRegistered()` non-
  reactive, pre-existing).
- Review (fresh reviewer, ticket + diff): PASS_WITH_NOTES. Verified:
  `punchWindowFrom` carries the former gate byte-for-byte and the
  private `loopRegion()` is gone with no callers; `placeTakes` still
  crops from the window frozen at press; `syncArmed` re-derives the
  engine's arm list from current clips on EVERY press (so the mirror's
  staleness never reaches a real press); the no-mic pass keeps
  `startInFlight`/`stopPending`/`startTransport`/`transportRolled` and
  the byte-identical status sequence; the count-in path unchanged;
  `ContextMenu`'s change is additive with no caller passing `checked`;
  the plate listener, both homes, no `aria-haspopup`, the gesture named
  on `aria-label` and `title` in all four modes, the three comments
  rewritten, the 2xl select + label intact, `R` untouched; the overlay
  geometry/border mapping exactly as specified under the `z-30`
  playhead; `App.tsx` collapsed onto `currentPassPunchWindow`; no new
  forbidden Tailwind forms; the measurement figures match batch 8's
  record. Findings → one rework (in flight): **major** — the keyboard
  opener opens a menu a keyboard user cannot operate (`ContextMenu`
  portals to body with no focus management; Tab leaves the footer) →
  focus the first item on open, arrow roving, Enter/Space, focus
  return on close, additive; **minor** — `currentPassPunchWindow()`
  returns the PREVIOUS pass's window at rest (clear at `finishPass`);
  two unpinned branches (unknown id kept; all tracks disarmed mid-pass
  with the engine open — safe by `recordingEngine`'s `disarm`/`stop`
  contract); the `KNOWN GAP` comment (already fixed by the lead);
  **nits** — arming a track with only `instrumentProgram` now records
  no audio at all (the ticket's §3b; feedback is `RECORD: no MIDI
  received`); re-right-click closes rather than re-anchors (shared
  primitive, pre-existing); one import line in `App.tsx`.
- **T26 rework: DONE** (six files, +638/−92). `ContextMenu` (additive):
  on open the opener is captured and the first ENABLED row focused on
  the next frame (after the layout clamp, so a row parked at −9999
  cannot scroll the page); ArrowDown/Up wrap, Home/End; Enter/Space
  left to the native buttons (a handler would fire `onSelect` twice);
  on close focus returns to the opener only when focus was lost or is
  still inside the menu (an `onSelect` that opened a modal keeps it).
  `passPunchWindow` cleared at the end of `finishPass` (mutation-
  checked). Three new pins: an unknown armed id is kept by the engine;
  a pass disarmed mid-pass still stops the engine and lands its take
  (the decision is frozen in `beginPass`); `currentPassPunchWindow()`
  is null at rest. Browser check on 52678 (live key, 1024): Shift+F10
  → `Punch off` focused → ArrowDown/End/wrap both ways → activate →
  select `in-out`, aria-label updated, focus returned to RECORD. tsc 0;
  suite green; greps 0. Every existing `ContextMenu` caller now shows
  a focus ring on its first row when opened (the WAI-ARIA pattern);
  no focus trap and no `aria-labelledby` on the menu (pre-existing
  shape — next increment).

## T25 — what landed (DONE_WITH_CONCERNS; 143/0; ruff clean; pytest 22 + 4)

- NEW `lib/followAction.ts` + test, `launchQueue.ts` + test,
  `DawSessionGrid.tsx`, `dawImportClient.ts`, `projectClient.ts`,
  `tasmoToSession.ts` + NEW `tasmoToSession.follow.test.ts`,
  `tasmo_project.py` + NEW `tests/test_tasmo_follow.py` (+717/−31 tracked).
  `nextFollow(state, rule, rng)` decides WHAT, `dueAt(rule, …)` decides
  WHEN; `next` at the last occupied row / `prev` at the first → `null`
  (Tracktion's `trackNext`/`trackPrevious` produce no handle at the ends;
  wrap is `trackRoundRobin`, stop is `globalStop` — cited `.h:20-52`,
  `.cpp:43-51, :174-207, :282-305`, nothing copied); `other` with one
  row → `null`; `any` includes the current row; `dueAt` floors at one
  beat / one play (a zero rule would refire every 15 ms tick).
  `LaunchSpec.at?` used only when finite, the `nextGrid` default and
  non-finite guard intact, everything else in the queue unchanged.
  Grid: `followRef` armed in `fireTicket`'s `start()` from `ticket.at`,
  a separate `playCountRef` (survives the follow disarming itself so
  `again` + `{plays}` counts a relaunch), the pump stays up while a
  follow is armed and fires THROUGH `queueLaunch` with the explicit
  `at`; the rule is written onto the imported `DawClip` (the only path
  into `dawProjectToTasmo`; `dawImportStore` is outside the write set)
  with a local version counter; an edit while playing patches the
  armed follow. Persistence: `followAction` → `TasmoClipInput` →
  backend `FollowAction`/`FollowAfter` (every field defaulted) →
  `TasmoLoadedClip` → `tasmoToSession` with `parseFollowAction` turning
  anything unusable into no rule. Right-click menu on an occupied cell
  (presets + `Follow action…`) with an inline editor strip under the
  toolbar (native selects/range/number with labels). TDD on every unit
  (each red first). tsc 0; greps 0; `npm test` 143/0; ruff clean; pytest
  `test_tasmo_follow` + `test_vst_daw_tasmo` 22 passed, routing 4.
- **The concern:** the browser check was NOT done — the builder cited
  the user's live backend on 8600 and `vite.config.ts`'s `strictPort:
  true`/hardcoded proxy (outside its write set). Lead's note: other
  builders this batch ran Vite on free ports with `--port`; the
  substitute evidence is a deterministic end-to-end pin against the
  real `beatClock` (launch at t=4, "Next after 1 bar" → `dueAt 6`, a real
  bar line, the queue takes `at=6` verbatim and hands it back at exactly
  6, while the same launch through the grid lands at 8 under a coarser
  quantize) — the sample-exact handover the check would have listened
  for. The reviewer judges whether the block was real. Follow-ups: the
  deadline is polled on the 15 ms pump (a hard-backgrounded tab fires
  late, as a queued press already does); no `.als` parser reads follow
  rules; the editor is a strip, not a popover.
- Review (fresh reviewer, ticket + diff + the Tracktion sources): **FAIL**.
  Verified: scope; `at?` honoured only when finite with everything else
  in the queue unchanged; every `nextFollow` decision pinned; the
  Tracktion citation matches the source (nothing textual shared); the
  mutation of the imported clip reaches the object `dawProjectToTasmo`
  saves (`performTracks` filters, never copies); `projectImport` still
  passes the whole payload; backend defaults and the dump/legacy pins;
  rule 3 on every editor control; greps 0; the timing line is DEV-only.
  Findings → one rework (in flight): **major** — a user press on a
  column with an armed follow is silently DISCARDED: `followRef` is
  never cleared by a press, the follow tick runs before `advance`, and
  its `queueLaunch` replaces the pending press ticket when both target
  the same line (one-line fix: a non-follow press deletes the column's
  armed follow); **major** — the browser check was skipped and the
  stated blocker does not hold (`npx vite --port` overrides the config;
  the proxy serves reads) — the substitute pin exercises no part of
  the grid, which is where the first finding lives; **minor** —
  `parseAfter` turns the backend's null-filled partial rule into a
  per-beat rule (`Number(null) === 0`); `dueAt` uses the unsanitised
  `beatSec` in the offset; `playsDone/elapsedSec/lengthSec` are
  threaded but never read; **nits** — bar-0 clock reads rest on
  `claimClock`'s single-segment map; a disabled (unplayable) cell has
  no menu; the editor ids are document-global (the existing precedent).
- **T25 rework: DONE** (+732/−31 tracked). A non-follow `queueLaunch`
  deletes the column's armed follow (it re-arms in `start()` from the
  clip that fires). `asNumber()` treats null/undefined as absent — a
  real bug: the backend dumps a period-less rule as three nulls, which
  loaded as a per-beat rule firing forever; pinned both ways. `dueAt`
  uses the sanitised beat (pinned with NaN/0 `beatSec`). The unread
  `FollowState` fields kept as the ticket's contract and documented as
  bookkeeping ("WHAT VS WHEN"); the bar-0 comment added. **Browser check
  done** on 5399 (`npx vite --port` overrode the config; the user's
  backend on 8600 untouched, healthy before and after; a two-cell
  fixture in `%TEMP%`; rules set through the UI since that backend
  predates `follow_action`): `[perform] follow col 0 row 0 -> row 1
  at=50.5120 now=50.4800 lead=0.0320`, `-> stop at=92.4907`, `-> row 1
  at=134.4907 … 158.4907` — every `lead` positive (29–40 ms inside the
  50 ms window, so `fireTicket` hands `ticket.at` exactly to both the
  incoming and outgoing sources) and every `at` on a real bar line
  (differences exact multiples of 2.000 s). Stop stopped. The finding-1
  case: Cell A pressed 706 ms into the bar with `Next` armed → after the
  line Cell A playing, Cell B not, zero follow lines; the column re-armed
  one bar later. tsc 0; the three suites green; greps 0.

## Wave 2a — Reviews

Every ticket had a fresh-context `opus-reviewer` on ticket + diff (+ new
files). T24 PASS_WITH_NOTES → rework (liveMixer-side pins with an
analyser-capable fake, the master-hold cost documented, two comments);
T25 **FAIL** → rework (a press on a column with an armed follow was
silently replaced by the follow's own ticket — one line; the skipped
browser check run for real on 5399 with the timing lines; `Number(null)
=== 0` turning a period-less saved rule into a per-beat one — a real
bug; the sanitised beat; the unread state fields documented); T26
PASS_WITH_NOTES → rework (a keyboard-operable `ContextMenu`: first row
focused, arrow roving, focus return; the pass window cleared at rest;
three pins) + the `requestAnimationFrame` guard that T24's full run
caught; T27 PASS_WITH_NOTES → rework (unmount cleanup on both pads,
order-sensitive test asserts, disabled-knob guard). Lead integration
edits: `automationGesture.ts` and `WaveformEditor.tsx` gesture comments
(T27's reviewer), `midiCapture.ts`'s `KNOWN GAP` paragraph (closed by
T26). Two builders hit their turn limits at the browser check (T26 at
100, T27 at 60) and were resumed with the check bounded.

## Wave 2a — Verification (lead-run on the converged tree — the commit gate)

Run after the last rework (T25's), from the worktree:

- Protected files untouched (`git diff --exit-code --stat` empty): the
  wave-1 list plus `editorStore.ts`, `renderCore.ts`, `projectImport.ts`,
  `editorAutosave.ts`, `gestureTracker.ts`, `levels/`, `vite.config.ts`,
  `docs/guides/`; the two existing tasmo test files untouched.
- Backend touched (`tasmo_project.py`): `uv run ruff check .` → all
  checks passed; `ruff format --check` → 380 files already formatted;
  `pytest tests/test_tasmo_follow.py tests/test_tasmo_routing.py
  tests/test_vst_daw_tasmo.py -q` → 26 passed.
- Changed (tracked): 24 files incl. `App.tsx`, `EffectWindows.tsx`,
  `FxRack.tsx`, `MixerStrips.tsx`, `OwlPad.tsx`, `PlayerFooter.tsx`,
  `SpatializerPad.tsx`, `WaveformEditor.tsx`, `effects/EffectControls.tsx`
  + `EffectKnob.tsx` + `EffectXYPad.tsx`, `DawSessionGrid.tsx`,
  `ContextMenu.tsx`, `automationGesture.ts`, `dawImportClient.ts`,
  `launchQueue.ts` + test, `midiCapture.ts`, `projectClient.ts`,
  `tasmoToSession.ts`, `liveMixer.ts`, `recordingStore.ts` + test,
  `tasmo_project.py`. New: `effects/EffectKnob.gesture.test.ts`,
  `lib/followAction.ts` + test, `lib/tasmoToSession.follow.test.ts`,
  `state/stripMeters.ts` + test, `tests/test_tasmo_follow.py`.
- Four CLAUDE.md Tailwind v4 greps → `0 0 0 0` on all thirteen touched
  `.tsx` files (whole files).
- `npx tsc --noEmit -p tsconfig.json` → clean; `npm run lint:scripts` →
  clean; `scripts/screenshots/tsconfig.json` → clean.
- NUL / BOM / CRLF sweep over the 32 changed and new paths: ONE hit —
  `liveMixer.ts` carried a raw NUL byte inside a string literal
  (`sig: '<NUL>'`, T19's "no chain yet" sentinel written as the control
  character instead of the `'\0'` escape). **Correction to the wave-1
  gate:** the same sweep flagged the file then and the lead misread it
  as a CRLF hit, "normalised" a file that had no CRLF, and committed the
  NUL in `28b1dae`. Fixed at this gate by replacing the literal with the
  `'\0'` escape (identical string value; the four `liveMixer.*` suites
  and tsc unchanged). The banned word appears in no added line and no
  new file.
- `npm test` → **143 passed, 0 failed** (exit 0). Was 139/0 at `28b1dae`;
  the four new suites are this wave's.
- `node scripts/ab-bounce/run.mjs` → PASS, every case within 1e-4, every
  shape equal (unchanged from wave 1 — `renderCore.ts` untouched).
- Beyond the suite: T24's five-meter arithmetic and live-rewire check on
  45220; T25's follow timing lines on 5399 incl. the press-wins case;
  T26's width measurements (byte-for-byte batch 8) and keyboard path on
  23616/52678; T27's headless real-component suite (its browser lane
  check remains unobserved).

## Wave 2a — Unresolved risk

- Rack controls: a live automation lane has still not been WATCHED
  closing one pass per drag (T27's headless suite is the evidence);
  `SpatializerPad`'s motion select/presets and `OwlPad`'s program
  select/HOLD still write with no boundary.
- `ContextMenu`: every caller now shows a focus ring on the first row
  when opened; no focus trap, no `aria-labelledby`, re-right-click
  closes rather than re-anchors; a dead RECORD key has no keyboard
  route to the punch menu.
- Arming a track with only `instrumentProgram` records no audio at all
  (the design; feedback is `RECORD: no MIDI received`).
- Strip analysers are created with the strips (constant cost whether
  or not the drawer is open); the bar colour is the inherited red;
  opening the drawer starts the master LUFS integration and closing it
  as the last holder clears the holds.
- Follow actions: the deadline is polled on the 15 ms pump; no `.als`
  parser reads follow rules; the editor is a strip under the toolbar;
  the persistence round trip is pinned in halves (unit + backend), not
  across the real API.

## Design decisions (lead)

- T19 was split at dispatch: its offline mirror became T19b, run after
  T18, because two wave-1 tickets would otherwise have shared
  `renderCore.ts`. Two workers never share a file; the split cost one
  extra review cycle and no rework.
- Pan/in-chain lead = the track's OWN insert-chain latency (T19's
  deviation), not the whole path to the sum: the panner precedes the
  comp and every downstream bus. Offline the same rule, over the chain
  actually built, gated on `includeFx`.
- Undo coalescing keys are derived at the CONTROL level (T23's
  deviation): an armed ride writes the track and the lane every frame,
  so per-target keys would have made one step per frame. Anonymous
  writes share one null identity (pinned by the raw-`setState` and
  `placeTakes` cases); `{ coalesce: true }` stays a join-anything
  override (pinned by the bus-fader + send-knob gesture). A hold frame
  joins only a NAMED open step.
- An armed EMPTY track does not capture MIDI (T22): the mic engine
  already lands a take on every armed track. The mic-side skip for
  instrument tracks is T26's (wave 2a), with `capturesMidi` the one
  definition both sides import.
- NodeF.I.'s audio-rate branch stays on its own wiring (T20): a mod
  port takes an `AudioNode` and the lfo node's oscillator is shared and
  `t0`-locked. A node-port `ModTarget` variant is a follow-up.
- The comp `DelayNode` carries `channelCount = 2` +
  `channelCountMode = 'explicit'` offline AND live: T18 measured the
  tail loss through a panner, so the pin is the guarantee.
- The offline render pads the context by the declared-latency bound
  for the MASTER scope only; stems/selections keep T14's truncation,
  pinned, pending a call on the harness's case-C reference.

## Wave 1 — Reviews

Every ticket had a fresh-context `opus-reviewer` on ticket + diff (+ new
files); verdicts and dispositions are inline above. Summary: T18
PASS_WITH_NOTES → rework (tail padding, the measured channel-count
reconciliation, pins, the harness reference fix from T19b); T19
PASS_WITH_NOTES → rework (header residual for the master rack, a
docblock, a master-only refresh) + the comp-pin addendum; T19b
PASS_WITH_NOTES → test-only rework (bus-routed, master-lane, two-
latency pins); T20 PASS_WITH_NOTES → rework (`invalidate` after an
inspector edit — a real stall reproduced in the app, the MIT notice
completed, the DC leg tested); T21 PASS_WITH_NOTES → rework (the roll
swallows `c/x/v/d` once it owns the key so the timeline cannot cut
clips underneath, paste advances, text-selection escape, ref reset);
T22 PASS_WITH_NOTES → rework (decodable placeholder + seeded peaks,
program resolved before the add, the mic double-take documented as a
T26 hand-off); T23 **FAIL** → rework (key/flag consumed at the top of
the recorder, `advanceAutomationHolds` arms only when holds exist and
joins only a named step, bus key shape, five new pins). Three
reviewers/investigators exhausted their turn budget before writing
(the takes, metering and gesture recon; the T19b review) and were told
to write from what they had read; none was resumed with new reading.

## Wave 1 — Verification (lead-run on the converged tree — the commit gate)

Run after the last rework (T18's), from the worktree:

- Protected files untouched (`git diff --exit-code --stat` empty):
  `backend/`, `tests/`, `pyproject.toml`, `uv.lock`, `.github/`,
  `electron-ui/`, `tempoMap.ts`, `beatClock.ts`, `decodeCache.ts`,
  `playerStore.ts`, `routingGraph.ts`, `automationModes.ts`,
  `rackEffects.ts`, `recordingStore.ts`, `recordingEngine.ts`,
  `levelsStore.ts`, `midiBus.ts`, `pianoRollStore.ts`, `midiSynth.ts`,
  `docs/guides/`. No backend change → ruff/pytest not required.
- Changed (tracked): `ab-bounce/README.md` + `main.ts`, `App.tsx`,
  `PianoRoll.tsx`, `nodefiLive.ts`, `renderCore.ts` + routing test,
  `editorStore.ts`, `liveMixer.ts` + envelope + latency tests — 11
  files. New: `lib/midiCapture.ts` + test, `lib/modulation.ts` + test,
  `lib/noteClipboard.ts` + test, `lib/renderCore.automation.test.ts`,
  `state/editorStore.coalesce.test.ts`, this record.
- Four CLAUDE.md Tailwind v4 greps → `0 0 0 0` on both touched `.tsx`
  files (whole files).
- NUL / BOM / CRLF sweep over the 20 changed and new paths → clean
  (`liveMixer.ts` was normalised to LF at the gate); the banned word
  appears in no added line and no new file.
- `npx tsc --noEmit -p tsconfig.json` → clean; `npm run lint:scripts` →
  clean; `scripts/screenshots/tsconfig.json` → clean.
- `npm test` → **139 passed, 0 failed** (exit 0). Was 134/0 at baseline
  `b66886c`; the five new suites are this wave's.
- `node scripts/ab-bounce/run.mjs` → PASS, every case within 1e-4,
  every shape equal: A ≤1e-6, B (×4) ≤1e-6, C (×3) and D BIT-IDENTICAL,
  E (the new per-track comp case) BIT-IDENTICAL.
- Beyond the suite: T20's and T22's browser checks (real app, synthetic
  inputs, free ports 2877/55565 and 5410/5411), T21's on 9950; T18's
  impulse and tail-loss measurements; T22's 12-mutant check; T19's and
  T19b's non-vacuity mutations are recorded inline.

## Wave 1 — Unresolved risk

- The mic engine still records an armed INSTRUMENT track alongside the
  MIDI capture (one press → two takes) until T26 lands the skip.
- A MIDI pass with the transport not rolling collapses to step 0 (no
  `transportRolled` repair on the MIDI path); punched MIDI takes are
  destructive; N armed MIDI tracks capture one stream.
- Stems and selections-with-FX still lose their last `own` seconds to
  the trim's zero-fill (pre-existing T14, now pinned); a master-rack
  lane is still early by `maxSec` live and by the trim offline (stated
  in both headers); sends remain uncompensated.
- The piano roll's clipboard is single-note in the UI (the store's
  selection model); two clipboard ops within 300 ms fold.
- NodeF.I.'s audio-rate mod edges are outside the modulation engine;
  no mod-matrix UI yet.
- Truly interleaved two-pointer gestures still split per alternation
  (a linear snapshot stack).

## Reviews

(pending)

## Verification (lead-run on the converged tree — the commit gate)

(pending)

## Unresolved risk

(pending)
