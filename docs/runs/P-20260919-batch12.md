# P-20260919-batch12 — open fixes from the verified backlog, and one-line consolidation

Baseline: `personal/main` = `370e659`. Worktree `C:\Users\skream\projects\_thedaw-batch12`, branch `feat/batch-12`.
Source of the item list: read-only recon on 2026-09-19 that checked every open item in BACKLOG.md, IN-THE-WORKS.md,
HANDOFF.md, design/feature-gap-plan.md, design/repair-expansion-report-and-plan.md, reference/edit-tab-feature-gap,
the handoff guides (F01–F25), docs/runs follow-ups and orchestration records against the code at 370e659
(88 items already done in code were dropped). Evidence `file:line` below is from that recon — builders re-read before editing.

## Order
1. **Phase A — land the uncommitted batch-11 assistant work** (worktree `_thedaw-batch11`): effort dropdown, context meter,
   live Claude model list, stuck-approval fix, Foundry-style chat bubbles, Foundry sidecar stop fix, Foundry orb panel.
   Its Foundry permission-mode part failed audit (CLI still launched with the skip flag) and is being reworked; lands after
   a fresh audit PASS. Then commit → `personal/main`.
2. **Phase B — this batch.** Tickets below run in parallel; each has a disjoint write set. Sonnet builds, an Opus auditor
   reviews every ticket, confirmed findings go back to the same builder.
3. `personal/main` is merged into `feat/batch-12` after Phase A; the **A-tickets** (assistant area) start only then.
4. Integrate: full backend suite, frontend tsc/tests/lint/lint:classes, Foundry tsc/vitest, ruff check + format.
5. Land on `personal/main`, fast-forward the local checkout, fast-forward stale fork branches, archive worktree
   leftovers into `_backup/` and remove merged worktrees.

## Rules every ticket follows
- Work ONLY in `C:\Users\skream\projects\_thedaw-batch12`, ONLY inside the ticket's write set. Other builders edit other
  files at the same time: a type error or failing test in a file you do not own is theirs — report it, do not fix it.
- No git command that changes state (no add/commit/checkout/reset/stash/restore/merge/rebase/clean/mv). The lead commits.
- Never delete a file. Retiring a file = move it under `deprecated/` (root, git-ignored) or `backend/deprecated/` and say so.
- No new dependencies. No lockfile edits. Never launch the app, Electron, a browser window or a dialog. Never use port 3000
  or 8765. Backups go under `_backup/` (git-ignored), never next to source.
- Test first. Own tests only, next to the code (`*.test.ts(x)` under `frontend/src/`, `tests/test_*.py`).
- Frontend: Tailwind v4 canonical classes; run `npm run lint:classes`. Accessibility HARD RULE 3 for any control.
- Python: `uv run ruff check <your files>` and `uv run ruff format --check <your files>`; pytest with
  `--basetemp=C:/Users/skream/AppData/Local/Temp/claude/pt-b12-<ticket>` and `-p no:cacheprovider`.
- Model ids are never written from memory (CLAUDE.md HARD RULE 1).
- Report: status line (DONE / DONE_WITH_CONCERNS / BLOCKED), per item "fixed at file:line + test name", exact command output.

## Tickets

### Backend
**T01 SERVER** — write set: `backend/server.py`, `tests/test_server_b12.py`
- BE-004: module toggle PATCH reads/writes `module.json` without an encoding (server.py:1151-1153) → utf-8 both ways.
- MAKE seed: `Form(-1)` seeds (server.py:1651, 2218) are never resolved. Resolve -1 to a concrete random 32-bit seed
  before generating, use it, and return it: add `"seed": <int>` to the generation response JSON and to the saved metadata.
  CONTRACT for T17: response/metadata key `seed` (int, the seed actually used).
- A01: `GET /api/build-info` → `{ "commit": <git sha or null>, "built": <iso or null>, "version": <pyproject version> }`
  (read `git rev-parse` once at startup, tolerate absence).

**T02 SECURITY** (starts after T01) — write set: `backend/lib/cross_site.py`, `backend/modules/genaiproxy/access.py`,
`backend/modules/project/router.py`, `backend/modules/plugin/router.py`, `backend/modules/plugin/gan_file.py`,
`backend/server.py` (only the middleware/CORS lines), tests.
- SEC-001: genai-proxy spends the Gemini key for any LAN client with no browser headers (access.py:18-21) → a non-loopback
  caller must present the launch token (`backend/lib/launch_token.py`).
- ITW security P1: gate project save with `embed_audio` (project/router.py:152,190) and plugin reveal (plugin/router.py:386)
  with the same launch-token check `places/router.py:85` uses; loopback callers unaffected.
- SEC-003: `.gan` path guard uses a string prefix (gan_file.py:148, plugin/router.py:452) → resolved-path containment.
- CORS `allow_origins=["*"]` with credentials (server.py:69-70): restrict to loopback origins (any port) plus the app's own
  scheme; keep LAN access working for token-bearing requests. Explain the chosen rule in a comment.

**T03 VST BACKEND** — write set: `backend/modules/vst/router.py`, `backend/modules/vst/live_host.py`,
`backend/modules/vst/scanner.py`, `tests/test_vst_b12.py`
- LAN2: `/live/session` answers non-loopback callers (router.py:1117-1118) → 409 for non-loopback.
- R5-2: any `.vst3` path accepted incl. UNC (live_host.py:451-463) → only paths under the scanned VST3 roots; reject UNC.
- VST-003: scan-cache signature samples one directory level (`scan_roots_signature`) → include nested bundle dirs.
- VST-001: tests for the host funnel / bundle resolution / raw_state round-trip that exist in code today.

**T04 LIBRARY STORE** — write set: `backend/modules/library/db.py`, `backend/modules/library/store.py`,
`backend/modules/library/router.py`, `tests/test_library_b12.py`
- LIB-001: `upsert_entry` resets analysis statuses to pending (db.py:722-724, 778-780) → preserve existing statuses.
- LIB-002: `reindex` never enqueues analysis (store.py:1670) → call `_maybe_enqueue_analysis` for new/changed entries.
- LIB-004: unpaged list path `SELECT * FROM analysis` (db.py:1791; router.py:121-140) → the narrowed column set the paged
  path uses.
- SCORE-001 support: add the notation identity fields the DETAILS identity form sends (read
  `frontend/src/components/layout/DetailsView.tsx:131-146` for the exact field names) to `USER_MUTABLE_FIELDS`
  (store.py:55). CONTRACT for T08: the field names you add, listed in your report.

**T05 ANALYSIS** — write set: `backend/modules/analysis/engine.py`, `backend/modules/analysis/module.json`,
`tests/test_analysis_b12.py`
- LIB-003: `loudness_lufs` is only passed through, never computed (engine.py:146) → compute integrated loudness with what
  the venv already has (check `uv.lock`: pyloudnorm, or ffmpeg `ebur128` via `backend/lib`); audio I/O only via
  `backend/lib/audio_io.py`.
- LIB-007: module.json:9 says "opt-in" — make it describe actual behaviour.

**T06 SUNO PROMOTE** — write set: `scripts/promote_suno_stage.py`, `backend/modules/library/suno_promote.py`,
`scripts/ingest_suno_cache.py` (move only), `tests/test_suno_promote_b12.py`
- L1: dry run opens the library DB read-write (promote_suno_stage.py:197) → `mode=ro` in dry run.
- L2: dry run reports every lineage edge unresolved (suno_promote.py:1304-1314) → resolve against the ids the run would
  create.
- F12: move the legacy title-dedupe `scripts/ingest_suno_cache.py` to `deprecated/` (it is superseded by staging).

**T07 LYRIA** — write set: `backend/modules/lyria/sidecar.py`, `backend/modules/storage/router.py`, `tests/test_lyria_b12.py`
- INT-001: adopts any listener on its port and reports Mock (sidecar.py:586-589) → adopt only after an identity/health
  check that proves it is our Lyria sidecar.
- INT-003: holds its lock across `npm install` (sidecar.py:580) → install outside the lock.
- INT-004: storage "ready" ignores `listening` (storage/router.py:693).

**T08 NOTATION ROUTES** (starts after T04) — write set: `backend/modules/notation/router.py`,
`backend/modules/sheetimport/router.py`, `backend/modules/sheetimport/parser.py`, `tests/test_notation_routes_b12.py`
- SCORE-001: DETAILS identity UI saves nothing (DetailsView.tsx:131-146; no route) → add the route the UI calls, persisting
  via the library store fields T04 added.
- SCORE-008: `/pack` re-engraves the PDF every click (router.py:769) → reuse an existing, newer artifact.
- SCORE-009: artifact GET writes to the DB (router.py:180) → reads must not write.
- SEC-005: sheetimport `/parse-path` reads any path (sheetimport/router.py:68-73) → library root / allowed dirs only.

**T09 NOTATION ENGINE** — write set: `backend/modules/notation/engine.py`, `backend/modules/notation/identity.py`,
`tests/test_notation_engine_b12.py`
- SCORE-002: `identity.py` is unwired → use it for `artist_name()` (engine.py:1325, 1404).
- SCORE-005: note chart audio block empty (`write_notechart`/`build_notechart` called without `audio=`, engine.py:1321,1401).
- SCORE-007: capabilities list `midi`/`json`/`alphatex` that `/export` rejects (engine.py:350-355 vs router.py:310) →
  capabilities must equal what export accepts.
- SCORE-013: tab PDF export routed through music21 (engine.py ~1002) → use the tab-capable path.

**T10 NOTATION BACKFILL + PDF + UNITY** — write set: `backend/modules/notation/backfill.py`,
`backend/modules/notation/pdf_render.py`, the Unity `NoteChartLoader.cs` and its README, `tests/test_notation_misc_b12.py`
- SCORE-010: backfill id collision (backfill.py:169). SCORE-011: plain vs escaped title loop (`_needs_fix`, backfill.py:71).
- SCORE-012: tab PDFs never show artist (pdf_render.py:197). SCORE-016: `PDF_RENDERABLE_SUFFIXES` unused (pdf_render.py:48).
- SCORE-006: Unity loader default filename mismatch (NoteChartLoader.cs:35) + README.

**T11 BACKEND MISC** — write set: `backend/modules/backup/service.py`, `backend/core/background_workers.py`,
`backend/core/jobs.py`, `backend/modules/effects/router.py`, `tests/test_backend_misc_b12.py`
- BE-005: backup import reports `done` after skipped writes (service.py:467-477).
- BE-006: background job table never pruned, O(n²) enqueue (background_workers.py:117,146) → bounded, O(1) enqueue.
- BE-012: `core/jobs` table unbounded; `list_jobs` dead (jobs.py:66) → bound the table; keep `subscribe`.
- BE-008: `/api/studio/process` blocking `read_bytes` in async route (effects/router.py:499) → `asyncio.to_thread`.

**T12 DSP A** — write set: `backend/modules/enhance/router.py`, `backend/modules/restoration/dsp.py`,
`backend/modules/creative_neural/router.py`, `backend/modules/creative_neural/dsp.py`, `tests/test_dsp_a_b12.py`
- FX-002: Un-Crush `afftdn nr=0` at a low knob → 500 (enhance/router.py:56-60) → clamp to ffmpeg's valid minimum.
- FX-004: restoration knobs that do nothing (dsp.py:96-100) → wire them, or remove them from the declared params.
- FX-005: creative_neural descriptions overclaim (router.py:282 "between two tracks" on a single input; :217 vs :50) →
  describe what the code does.
- FX-008: dead bare `max(...)` statement (dsp.py:58).

**T13 DSP B** — write set: `frontend/public/edit-modules/parametric-eq.html`, the mastering `_eq` implementation file,
`backend/modules/delivery/router.py`, `tests/test_dsp_b_b12.py`
- FX-003: EQ pages send 3 of 5 bands (parametric-eq.html:188-189) → send all 5; backend applies all 5.
- FX-006: delivery true-peak is a `print` (delivery/router.py:65-69) → measure and return it; Batch Export `parallelJobs`
  (router.py:278) must bound concurrency.

### Frontend
**T14 DJ** — write set: `frontend/src/views/DJView.tsx`, `frontend/src/state/djSamplerStore.ts`, `frontend/src/views/DJView.b12.test.ts`
- 16 `setFlash` messages are never rendered (DJView.tsx:673,826) → render them (transient status line).
- Automix never beatmatches: interval (DJView.tsx:1115,1142) captures a stale `syncDeck` (deps `[automixOn, automixRestart]`)
  → read the current sync function through a ref.
- Sampler per-pad gain/loop/choke unreachable (`setPadOpts` uncalled, djSamplerStore.ts:23) → per-pad controls.
- `DeckRack` defined, never rendered (DJView.tsx:1616) → remove the dead component (git keeps history; note it).
- FE-024: DJ inputs without id/name/label (DJView.tsx:2467,2666). FE-032-adjacent: the `soon` branch (DJView.tsx:2631) — make
  it honest.

**T15 MIDI IGNORE** — write set: `frontend/src/state/midiBus.ts`, `frontend/src/state/midiIgnoreStore.ts`, tests
- FE-010: `isMidiSigIgnored` / `isMidiMessageIgnored` exist but midiBus never calls them (midiBus.ts:24).

**T16 VJ + UNDERFIT + SLIDE** — write set: `frontend/src/views/VJView.tsx`, `frontend/src/views/UnderfitView.tsx`,
`frontend/src/components/**/SlidePanel.tsx`, tests
- VJ popped out: inbound messages dropped (VJView.tsx:228 accepts only the iframe window) → accept the pop-out window too.
- VJ failure `detail` set but never rendered (VJView.tsx:64,376).
- FE-016: hidden warm tabs poll forever (VJView.tsx:390-393 questcast 5 s; UnderfitView.tsx:98-101 ping) → pause when hidden.
- Underfit upstream updater endpoints (underfit/router.py:203,209) have no caller → add the check/update control.
- FE-014: SLIDE persists a `blob:` URL (SlidePanel.tsx:267) → persist something that survives reload.

**T17 MAKE** — write set: `frontend/src/components/**/actionKeyFace.ts`, `frontend/src/state/generateStore.ts`,
`frontend/src/state/generateParamsStore.ts`, `frontend/src/components/**/AdvancedGenPanel.tsx`, `LyriaPanel.tsx`,
`SunoGenPanel.tsx`, `frontend/src/lib/cloudModels.ts`, tests
- P0: footer CREATE with Suno/Lyria selected submits to local `/api/generate-jobs` (actionKeyFace.ts:218; no cloud branch in
  generateStore) → route to the selected provider's existing generate path.
- FE-005: RF-Inv init type always 501 (AdvancedGenPanel.tsx:803) → not offered while the backend returns 501.
- FE-006: Lyria/Suno panel model dropdown patches `model` only, skipping RF presets (LyriaPanel.tsx:166).
- DL auto-download toggle has no consumer (generateParamsStore.ts:244) → download finished takes when on.
- Seed: show the seed actually used (T01 contract: response/metadata `seed`) and make "reuse seed" use it.
- INT-005: Lyria offered without a checkout (cloudModels.ts:30) → gate on the storage/lyria status.
- FE-028: dead `lastAudioBlob` (generateStore.ts:1204) → remove.

**T18 VST STATE STORAGE** — write set: `frontend/src/state/effectChainStore.ts`, `frontend/src/state/vstEditorStore.ts`,
new `frontend/src/lib/vstStateStorage.ts`, tests
- FE-003: every MIX knob move re-serializes VST `raw_state` into localStorage (effectChainStore.ts:159,178,198) → keep
  `raw_state` out of the localStorage payload (IndexedDB or the existing project storage), keyed by entry id, loaded on demand.
- FE-004: `QuotaExceededError` while capturing VST state is swallowed (vstEditorStore.ts) → surface it.
- Existing saved chains must still load (migrate the old inline `raw_state` once).

**T19 MIXER UI** — write set: `frontend/src/components/**/EffectsVizPanel.tsx`, `MixerStrips.tsx`,
`frontend/public/edit-modules/module-kit.js`, `EffectGuiStage.tsx`, `frontend/src/views/MixView.tsx`, tests
- FE-022: MIX effects viz is a fixed decorative curve (EffectsVizPanel.tsx:38-41) → draw the chain's real response where
  the effect exposes it; otherwise say so.
- Bus rename UI (store supports it, editorStore.ts:831) in MixerStrips.
- Output routing for the edit-module windows (no `setSinkId` in module-kit.js) → follow the app's output device.
- MixView.tsx:618 "All" → icon, per the icon-only toolbar rule.

**T20 UI SHELL** — write set: `frontend/src/components/layout/DAWCenterPanel.tsx`, `frontend/src/components/**/Tooltip.tsx`,
`frontend/src/App.tsx`, `frontend/src/components/layout/Shell.tsx`, tests
- FE-012: only Perform has an error boundary (DAWCenterPanel.tsx:114-220) → one per center tab.
- FE-013: HoverTip not portalled (Tooltip.tsx:45) → portal so it is never clipped.
- FE-025: controller-profile table statically imported into first paint (App.tsx:59) → lazy.
- `thedaw:set-left-panel` is dispatched (LineageModal.tsx:3326; actionHandlers.ts:481,485) but nothing listens → Shell listens.

**T21 SEMANTIC WAVEFORM** — write set: `frontend/src/components/**/DJSemanticWaveform.tsx`, `SemanticWave.tsx`, tests
- FE-011: failed decode invisible (DJSemanticWaveform.tsx:393-395). FE-008: per-sample `getChannelData` (:28-31).
- FE-021: `transparentBg` covered by an opaque canvas fill (:262-267).

**T22 FETCH/CACHE HYGIENE** — write set: `frontend/src/lib/backendLocalProvider.ts`, `frontend/src/components/**/HfTokenField.tsx`,
`frontend/src/state/questMidiClient.ts`, tests
- FE-019: library audio blob cache unbounded (backendLocalProvider.ts:209-224) → size-bounded LRU.
- HfTokenField drops transport errors (HfTokenField.tsx:94-96).
- FE-020: questMidiClient hardcodes `ws://127.0.0.1:8600` (questMidiClient.ts:24-34) → derive from the page like the other
  clients do.

**T23 DEAD CODE** — write set: `frontend/src/components/**/WaveformPreview.tsx`, `frontend/src/state/assistantBridgeStore.ts`,
`frontend/src/lib/cloudModels.ts` is NOT yours (T17), `frontend/src/lib/fetchRetry.ts`, `frontend/src/state/storageQuotaStore.ts`,
`frontend/src/state/ioDevicesStore.ts` (dead exports only), `backend/core/sidecar.py`, `backend/core/model_registry.py`
- FE-026: orphan `WaveformPreview` → `deprecated/` (leave the wavesurfer dependency; no lockfile edits this batch).
- FE-027 `assistantBridgeStore`, dead exports (`fetchRetry.ts:27`, `storageQuotaStore.ts`, I/O `currentDeviceIds`,
  `followSurfaceSink`, `activeThruPortId`, `getCueSinkId`, `listAudioInputs`) — prove no importer with grep before removing.
- BE-009/010: `backend/core/sidecar.py`, `backend/core/model_registry.py` with no importers → `backend/deprecated/`.

**T24 TIMELINE STORE** — write set: `frontend/src/state/editorStore.ts`, `frontend/src/state/recordingStore.ts`, tests
- D16: peaks normalised per clip and from channel 0 only (editorStore.ts:3326,3341-3343) → all channels, max per bucket.
- T46H: one undo step per recording pass (`addTakeToClip` has no coalesce, :737; :2148 `beginUndoStep()` each call) → use
  the store's `undoGroup` so a whole pass is one step.
- T66B: `releaseDecoded` (lib/decodeCache.ts:349) never called → on clip removal and project close/load.
- Unify F4: two selection setters (`setSelectedClips` :780, `setSelectedClipIds` :802) → one; update callers inside your
  write set, list outside callers in your report (the lead routes them).
- Automation lanes can only be born by riding with WRITE armed → add an `addAutomationLane(target)` store action
  (UI in T25b).

**T25a EDIT A** — write set: `frontend/src/components/audio/WaveformEditor.tsx`, tests
- D18: Mixdown always forces Save As (WaveformEditor.tsx:500) → save to the known target when there is one.
- FE-007: `clip.peaks` decode restarts on every clips change (:3040-3056) → keyed per clip.
- F14: insert ALL stems of a parent in one undo step, in a folder, with the parent muted (inspector :3798 area).
**T25b EDIT B** (after T24 and T25a) — same write set + `frontend/src/components/**/AutomationLane.tsx`
- "Add automation lane" control using T24's `addAutomationLane`. Metronome volume control (`metronomeStore.ts:53`
  `setVolume` has no UI) placed next to the metronome toggle.
**T25c EXPORT DIALOG** (after T25b) — write set: new `frontend/src/components/audio/ExportDialog.tsx`,
`frontend/src/components/audio/WaveformEditor.tsx` (wiring only), tests
- F25 / ITW EDIT: export is one hardcoded WAV (WaveformEditor.tsx:266); `lib/render/exportDialogModel.ts` exists with no UI →
  an export dialog driven by that model (format, bit depth, range), using the existing encoders/delivery paths only.

**T26 SCORE UI** — write set: `frontend/src/components/**/ScoreView.tsx`, `SingScoreView.tsx`, `LyricAnalysisPane.tsx`, tests
- Part names on every system in PAGE view (no option in ScoreView.tsx).
- Meter map in SCORE / SING / STUDY (library menu, DETAILS and INFO already have it: trackMenuActions.ts:906, RhythmBlock.tsx:73).

**T27 CI** — write set: `.github/workflows/lint.yml`
- `lint:scripts` (frontend/package.json:16) is not run in CI → add the step next to the existing frontend lint.

**T28 AUTOSAVE NOTICE** — write set: `frontend/src/components/**/AutosaveRecoveryNotice.tsx` (+ its test)
- T57B: `useAutosaveRecoveryStore().ownership` has no UI reader → one-line notice when another window owns autosave.

**T29 ROLL QUANTIZE** — write set: `frontend/src/lib/rollClip.ts`, `frontend/src/components/**/PianoRoll.tsx`, tests
- T37B: quantize / swing / groove on the roll clip document (none in rollClip.ts) — reuse `frontend/src/lib/clipNotes`.

**T30 VJ SIDECAR IDENTITY** (found by T07) — write set: `backend/modules/vj/sidecar.py`, `tests/test_vj_sidecar_b12.py`
- Same INT-001 shape as Lyria: the VJ sidecar adopts any TCP listener on its port → adopt only after an identity check
  against a route the VJ server really serves (read the VJ-9000 server code; do not guess), else a clear error.

### Assistant area (start after Phase A is merged into feat/batch-12)
**A1 ASSISTANT BACKEND** — write set: `backend/modules/assistant/mcp_relay.py`, `backend/modules/assistant/claude_session.py`, tests
- Relay routes unauthenticated on a 0.0.0.0 bind with CORS `*` (mcp_relay.py:239,261) → loopback-only (the stdio child and
  the local browser are both loopback).
- A brand-new turn can take the slot ahead of queued turns (claude_session.py:702-708, 1535-1537) → hand the slot to the
  head waiter directly; test with a new turn arriving during the wake.
**A2 ASSISTANT FRONTEND** — write set: `frontend/src/orb-kit/AssistantPanel.tsx`, `frontend/src/orb-kit/transcript/**`,
`frontend/src/orb-kit/stream/types.ts`, `frontend/src/orb-kit/editorToolBridge.ts`, tests
- Unify F1: reference chips no longer echoed under the user's message → carry `references` on ChatMessage and render them.
- Unify F3: a queued send takes a later send's context (single-slot refs, AssistantPanel.tsx:354,358,943,950) → per-turn.
- Quick commands advertise non-features (AssistantPanel.tsx:131-137) → only real ones.
- Voice input uses Web Speech and ignores the chosen mic (AssistantPanel.tsx:506-518) → honour the chosen input or say so.
- Audio stretch uploads the whole source blob (editorToolBridge.ts:537,175) → send only the clip window
  (`extractWindow`, editorTools.ts:297) and map the result back.

## Lead-owned (not tickets)
- Machine script `C:\Users\skream\claude-bridge-watchdog.ps1` force-kills every listener on 8765 without checking what it is
  (it killed the backend on 2026-09-18 22:58 and 23:13) → only kill the claude-bridge process. Backed up first.
- Worktree leftovers: `_thedaw-batch9` (an abandoned take-groups design superseded by batch 10's clip-owned takes) and
  `E:\thedaw-build\wt-qa` (fully superseded; only a QA port override) → patches archived to `_backup/`, worktrees removed.

## Not in this batch (need a product decision or are multi-day features)
Product decisions: GH-107 product name, §3.13 engine-rate render policy, orb over a white backdrop (a/b), VirtuosoControls
`showInstrument`. Deferred by plan: §3.14 disk streaming / resampling / MPE, GH-127 FlashAttention 3.
Multi-day features (own plans): UX-001 Kouhai/Senpai (built under the user's guidance), UX-002 legibility + one-accent pass,
SCORE notation writing editor, ripple edit, F16 Edit↔Mix live binding, F23 lineage UI, F13 candidate workflows, D14 rolling
FX scheduler, per-track heights (T51b), timeline virtualization + peaks mipmap (FE-031, T21), EDIT meter-map ruler (T05),
LOOM phases 1–2, Ableton import gaps, example projects with stems/MIDI/lyrics, Lyria→library (INT-002), broadcast relay
(INT-006), VJ camera picker, LoRA training endpoints, ACE-Step / anira / SoulX / RipX (level-up 01/02/06/07), F1 vocal retune,
generative extend / variation ladder / SAME latent endpoints, 502 semantics sweep, DSP-module and view test suites.
Needs a live check on the user's machine: MIX rack applied twice, LEARN hops, Sway mirror arbitration, fade overlay shape,
F10 Ozone behaviour, the 26 coded-but-unseen items (LOOM v2/v3 etc.).
